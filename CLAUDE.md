# CLAUDE.md — Melodia

Instructions pour Claude Code dans ce dépôt. Gardé volontairement court : la référence de fond vit dans `docs/`, jamais dupliquée ici (voir [[docs/DOCUMENTATION_GUIDE.md]] pour la carte complète).

## Architecture et Dépendances (Graphify)
- Avant d'explorer l'arborescence du projet ou de chercher des fichiers à la main, lis le rapport dans `graphify-out/GRAPH_REPORT.md`.
- **Règle absolue :** Ne lis JAMAIS le fichier `graphify-out/graph.json` ni les fichiers `.json` du dossier `graphify-out/`. Utilise uniquement le rapport Markdown.

## Le projet en une phrase
Melodia est un client musical premium auto-hébergé pour Jellyfin (Jellyfin = source de données, pas le produit). Vision complète : `docs/PROJECT_CHARTER.md`.

## Avant de travailler ici
1. Relire `docs/` avant toute nouvelle phase ou décision structurante — règle absolue posée dans `docs/PROJECT_CHARTER.md` §7. Une contradiction avec un document existant se signale explicitement, jamais résolue en silence (voir `docs/DOCUMENTATION_GUIDE.md` §5).
2. Décision structurante (nouvelle dépendance de catégorie, changement d'architecture/convention) → un ADR d'abord (`docs/ADR_TEMPLATE.md`), jamais de changement silencieux d'un standard déjà acté.
3. Toute fonctionnalité passe par `docs/DEFINITION_OF_DONE.md` avant d'être considérée terminée.
4. Stack, conventions Git, standards de code : ne pas réinventer — voir `docs/TECH_STACK.md`, `docs/GIT_WORKFLOW.md`, `docs/CODING_STANDARDS.md`.

## Comment je travaille ici (accord de collaboration IA)
- Je ne suis pas un exécutant : avant de répondre, j'analyse la demande, relis mentalement les décisions déjà actées dans `docs/`, compare plusieurs approches et vérifie la cohérence avec l'architecture/le Design System/la performance/la sécurité/l'accessibilité avant de recommander — jamais la première solution venue sans comparaison.
- Je signale les meilleures alternatives même non demandées si elles apportent une vraie valeur (bénéfice, coût, risques explicités), sans jamais imposer un changement de portée sur une fondation du projet sans validation.
- Je refuse — en l'expliquant, avec une alternative — toute solution qui duplique du code, casse le Design System, viole une convention déjà actée, dégrade les performances ou complexifie inutilement l'architecture ; voir `docs/ENGINEERING_MANIFESTO.md` §2 pour la liste actionnable des anti-patterns déjà interdits, pas re-décidée à chaque tâche.
- Les responsabilités par type d'unité de code (composant/hook/service/page) sont déjà définies dans `docs/CODING_STANDARDS.md` §4 — je m'y conforme, je ne les redéfinis pas.
- Aucune décision arbitraire (couleur, durée, dépendance, architecture) sans justification — cohérent avec `docs/ENGINEERING_GUIDE.md` et `docs/ADR_TEMPLATE.md`.
- Une tâche n'est « terminée » que si elle satisfait `docs/DEFINITION_OF_DONE.md` **et** a été revue (lisibilité, duplication, perf, a11y, cohérence avec les documents fondateurs) — jamais seulement « ça fonctionne ».
- Honnêteté sans exception : je ne prétends jamais avoir vérifié ce qui ne l'a pas été, je n'invente jamais un résultat, je ne masque jamais une limite technique — si une incertitude existe, je la signale avec ce qui manque et comment la lever (cohérent avec les Honesty Rules déjà appliquées dans `docs/` — audit trail EXTRACTED/INFERRED/AMBIGUOUS, gaps signalés plutôt que masqués dans `docs/EXTREME_SCENARIOS.md`).

> **Documents référencés dans un cadrage mais toujours absents de `docs/`** : plus aucun fichier manquant. `PRODUCT_BIBLE.md`, `FEATURE_BIBLE.md`, `UX_BIBLE.md`, `BRAND_BIBLE.md`, `VISUAL_IDENTITY_SYSTEM.md` existent tous. `UX_GUIDELINES` est couvert par `docs/UX_PRINCIPLES.md`. `DESIGN_SYSTEM` reste `docs/DESIGN_SYSTEM_ARCHITECTURE.md` (technique). `INTERACTION_GUIDE`, `MOTION_BRANDING`, `ACCESSIBILITY_VISUAL_GUIDE`, `ART_DIRECTION` n'ont jamais été créés comme fichiers séparés — étendus dans leurs équivalents existants à chaque fois (voir le journal de phase). `MISSION.md`/`VALUES.md` redemandés en Phase 2 n'ont pas été dupliqués — `docs/MISSION.md` et `docs/PRODUCT_VALUES.md` §4 restent les sources uniques.
>
> **Statut de l'identité visuelle littérale (important, à ne pas perdre)** : `docs/COLOR_SYSTEM.md`, `docs/TYPOGRAPHY_GUIDE.md` et `docs/LOGO_GUIDE.md` existent depuis la Phase 2 volume 2 et contiennent des **propositions v1 concrètes** (valeurs hexadécimales, famille de police recommandée, direction de logo) — mais chacun porte l'avertissement explicite que ce sont des jugements de goût non validés visuellement, pas des décisions arrêtées. Ne jamais les traiter comme définitifs dans une implémentation réelle sans confirmation explicite de l'utilisateur après avoir vu un rendu concret.

> **Attention à la collision de nom « Phase 1 »** : `docs/ROADMAP.md` définit « Phase 1 » comme la phase d'ingénierie MVP (Web + Desktop, pas encore commencée). Le cadrage produit reçu séparément s'intitule aussi « Phase 1 — Product Bible » mais désigne une étape de définition produit, pas la même chose. Les deux sont numérotées « 1 » indépendamment — ne pas les confondre lors de la lecture du journal de phase ci-dessous.

## Où chercher quoi
Point d'entrée unique : `docs/TECHNICAL_BLUEPRINT.md` (synthèse, ne fait pas autorité seul). Carte complète et à jour : `docs/DOCUMENTATION_GUIDE.md` §1. En résumé : vision/objectifs/risques → `PROJECT_CHARTER.md` ; architecture système → `ARCHITECTURE_PRINCIPLES.md` / arborescence concrète → `ARCHITECTURE.md` ; stack → `TECH_STACK.md` / `STACK_DECISIONS.md` ; process de dev → `DEVELOPMENT_GUIDELINES.md` ; feuille de route → `ROADMAP.md`.

## Journal de phase
Section alimentée à la fin de chaque phase (voir `docs/ROADMAP.md` pour la définition des phases). Chaque entrée : ce qui a été livré, l'état réel du dépôt, ce qui reste ouvert — pas un résumé marketing.

### Phase 0 — Fondations (2026-08-03)
- Les 13 documents fondateurs de `docs/` rédigés et auto-revus (voir historique des révisions de chaque document).
- Décision structurante actée : Tauri 2 comme runtime unifié Desktop + Mobile (voir `docs/TECH_STACK.md` §0).
- Licence MIT retenue pour le dépôt (ADR formel encore à rédiger avant publication — voir `docs/PROJECT_CHARTER.md` §3.10).
- Aucun code applicatif écrit — normal et voulu, la Phase 0 est documentation uniquement.
- Poussé sur `origin/main` (commit `836a8cd`).
- Ouvert : ADR de licence, mise en place du squelette de dépôt (Vite/React/Tauri, CI de base) pour entrer en Phase 1.

### Phase 0.5 — Blueprint technique (2026-08-03)
- 15 documents supplémentaires rédigés dans `docs/`, élaboration concrète de la Phase 0 (aucune redécision, voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 28 documents).
- Contradiction signalée et tranchée avec l'utilisateur avant de continuer : le cadrage demandait une « architecture Next.js », incompatible avec la décision Tauri de Phase 0 (Server Components/Actions nécessitent un serveur Node persistant) — tranché en faveur du maintien de Vite + React SPA (voir `docs/FRONTEND_ARCHITECTURE.md`, note de cadrage).
- Décisions structurantes nouvelles : monorepo pnpm + Turborepo (`docs/STACK_DECISIONS.md` §3, arborescence dans `docs/ARCHITECTURE.md`) ; moteur de recherche FlexSearch (`docs/STACK_DECISIONS.md` §2) ; SDK officiel `@jellyfin/sdk` plutôt qu'un client maison (`docs/JELLYFIN_INTEGRATION.md` §1).
- Amendement documenté : bibliothèque de référence des budgets de performance relevée de 100k à 200k titres (`docs/PERFORMANCE_BUDGET.md`, `docs/PERFORMANCE_GUIDE.md`).
- Consolidation documentaire : ADR_GUIDE et QUALITY_GATES demandés par le cadrage n'ont pas été dupliqués aveuglément — `ADR_TEMPLATE.md` existant réutilisé tel quel, `QUALITY_GATES.md` scoping restreint aux gates automatisés en CI (distinct de `DEFINITION_OF_DONE.md`).
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade (à confirmer avec l'utilisateur avant push, comme pour la Phase 0).
- Ouvert : squelette effectif du monorepo (actuellement seulement décrit dans `docs/ARCHITECTURE.md`, pas encore créé sur disque), ADR de licence toujours en attente, entrée en Phase 1.

### Phase 1 — Product Bible, volume 1 (2026-08-03)
- 10 documents produit rédigés dans `docs/` (vision, mission, valeurs, analyse concurrentielle, personas, parcours, charte UX, règles produit, métriques de succès, bible de synthèse) — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 42 documents.
- Ne pas confondre avec la « Phase 1 » de `docs/ROADMAP.md` (MVP d'ingénierie, non commencée) — voir la note de collision de nom ci-dessus.
- Consolidation documentaire : `PRODUCT_ROADMAP.md` demandé par le cadrage n'a pas été créé comme fichier séparé — vue produit ajoutée à `docs/ROADMAP.md` existant à la place, pour éviter deux feuilles de route qui divergent.
- Analyse concurrentielle (`docs/COMPETITIVE_ANALYSIS.md`) couvrant 12 produits + Navidrome, explicitement marquée comme basée sur la connaissance du modèle (coupure janvier 2026) et non une vérification en direct — à revérifier avant toute décision de conception qui s'appuierait fortement dessus.
- Décisions produit identifiées comme encore ouvertes plutôt que tranchées arbitrairement (voir `docs/USER_JOURNEYS.md` §13 et la vue produit de `docs/ROADMAP.md`) : pondération de la lecture aléatoire, comportement du cache local à la déconnexion, statut engagé de la fonctionnalité de découverte interne, détection réseau automatique de serveur Jellyfin.
- Aucun code applicatif écrit — voulu, cette phase reste documentaire. Aucune décision d'identité visuelle prise (`BRAND_BIBLE` toujours différé).
- Non poussé sur `origin/main` à ce stade.
- Ouvert : les décisions produit listées ci-dessus, `BRAND_BIBLE.md` si le projet en a besoin avant le travail visuel concret, entrée effective en Phase 1 d'ingénierie (MVP).

### Phase 1 — Feature Bible, volume 2 (2026-08-03)
- 14 documents de spécification fonctionnelle rédigés dans `docs/` (lecteur, file, recherche, bibliothèque, playlists, découverte, statistiques, Wrapped, paramètres, erreurs, états vides, interactions, priorisation, synthèse) — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 56 documents.
- Clarification de gouvernance, pas un assouplissement silencieux : `docs/PRODUCT_RULES.md` §10 précise désormais que l'interdiction de collecte sans consentement vise la donnée qui **quitte l'appareil** (télémétrie serveur, reste strictement opt-in) — l'historique d'écoute purement local requis par Statistiques/Wrapped/Découverte est activé par défaut, mais reste consultable, supprimable et désactivable à tout moment sans dégrader le reste du produit.
- Consolidation : les sections « Albums », « Artistes », « Favoris » du cadrage n'ont pas eu de fichier dédié — intégrées à `docs/LIBRARY_SPECIFICATION.md` comme types de contenu de bibliothèque, pour éviter une fragmentation artificielle.
- `docs/COMPETITIVE_ANALYSIS.md` (Phase 1 volume 1) réutilisée comme référence pour l'auto-revue plutôt que refaite — voir `docs/FEATURE_ROADMAP.md` §4 pour la synthèse comparative de ce volume.
- Décisions produit encore ouvertes ajoutées à celles déjà suivies (voir `docs/FEATURE_BIBLE.md` §5) : seuil de comptage d'une écoute, fréquence de réévaluation des playlists intelligentes/Daily Mix, statut technique des playlists collaboratives, source de données des paroles synchronisées.
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : toutes les décisions produit listées ci-dessus et dans `docs/USER_JOURNEYS.md` §13, `BRAND_BIBLE.md` toujours différé, entrée effective en Phase 1 d'ingénierie (MVP) — aucun code n'a encore été écrit sur l'ensemble du projet.

### Phase 1 — UX Bible, volume 3 (2026-08-03)
- 11 nouveaux documents de spécification UX rédigés dans `docs/` (flux atomiques, écrans, navigation, animation, responsive, accessibilité, onboarding, patterns d'erreur/notification, traitement des états vides, wireframes textuels, synthèse) + extension de `docs/INTERACTION_GUIDELINES.md` — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 67 documents.
- Consolidation, pas de doublon : `INTERACTION_GUIDE.md` (nom quasi identique à `INTERACTION_GUIDELINES.md` du volume 2) n'a pas été créé — le fichier existant a été étendu (états disabled/success/warning/error + justification cognitive). `ERROR_EXPERIENCE.md` et `EMPTY_STATES_GUIDE.md` ont été créés mais scopés étroitement sur la couche pattern UI/illustration, sans redupliquer les listes cas-par-cas déjà dans `docs/ERROR_STATES.md` et `docs/EMPTY_STATES.md` (volume 2).
- `docs/MOTION_GUIDELINES.md` comble un vide identifié depuis la Phase 0.5 (référencé dans un cadrage antérieur, jamais créé) — durées et courbes concrètes justifiées par les conventions établies du motion design d'interface (Material Design, Apple HIG), pas inventées arbitrairement.
- `USER_FLOWS.md` (108 flux atomiques) distingué explicitement de `docs/USER_JOURNEYS.md` (12 parcours macro, volume 1) par le grain de détail — aucun des deux ne redécrit l'autre.
- Décisions UX encore ouvertes ajoutées à celles déjà suivies (voir `docs/UX_BIBLE.md` §5) : comportement du panneau latéral du lecteur en tablette paysage, fréquence exacte des conseils contextuels d'onboarding.
- Aucun code applicatif écrit — voulu, cette phase reste documentaire. Aucune valeur de couleur/typographie concrète introduite malgré la profondeur du volume (`BRAND_BIBLE.md` toujours différé, vérifié explicitement dans l'auto-audit).
- Non poussé sur `origin/main` à ce stade.
- Ouvert : toutes les décisions UX et produit listées ci-dessus et dans les volumes précédents, `BRAND_BIBLE.md` toujours différé, entrée effective en Phase 1 d'ingénierie (MVP) — le projet reste à 100 % documentaire à ce stade, aucune ligne de code applicatif n'existe encore.

### Phase 2 — Brand Bible, volume 1 (2026-08-03)
- 8 nouveaux documents de marque rédigés dans `docs/` (personnalité, positionnement, ton, vocabulaire, direction visuelle, principes, analyse de marques concurrentes, synthèse) — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 75 documents.
- Consolidation confirmée avec l'utilisateur avant de commencer : `MISSION.md` et `VALUES.md` redemandés par ce cadrage n'ont pas été créés — `docs/MISSION.md` et `docs/PRODUCT_VALUES.md` §4 (Phase 1) restent les sources uniques. `docs/PRODUCT_VALUES.md` §1/§2/§5 étendus pour renvoyer vers `docs/PERSONALITY.md`/`docs/VOICE_AND_TONE.md`/`docs/VOCABULARY.md`, désormais plus détaillés.
- `docs/VISUAL_DIRECTION.md` reste strictement directionnel (style, hiérarchie, lumière, profondeur) — **aucune valeur hexadécimale, police ou logo choisis**, cohérent avec la décision déjà actée de ne jamais fixer une identité visuelle littérale par extrapolation seule ; deux points explicitement laissés « à valider avec l'utilisateur » plutôt que tranchés (degré de chaleur du minimalisme, mode sombre par défaut ou non).
- `docs/COMPETITIVE_BRAND_ANALYSIS.md` marqué du même avertissement d'honnêteté que `docs/COMPETITIVE_ANALYSIS.md` (Phase 1) : connaissance du modèle, pas audit visuel en direct.
- Aucun code applicatif écrit — voulu, cette phase reste documentaire. Aucune décision d'identité visuelle littérale prise.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : les deux points de direction visuelle à valider avec l'utilisateur, `BRAND_BIBLE.md` volume 2 (identité visuelle littérale) si le projet en a besoin avant le travail visuel concret, toutes les décisions produit/UX déjà listées dans les phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP) — le projet reste à 100 % documentaire, aucune ligne de code applicatif n'existe encore. Extraction du graphe de connaissances également interrompue par une limite de session API (2 chunks sur 3 en attente de reprise).

### Phase 2 — Visual Identity System, volume 2 (2026-08-03)
- 11 nouveaux documents visuels rédigés dans `docs/` (tokens, couleur, typographie, icônes, illustration, layout, surfaces, thèmes, logo, son) + 3 extensions (`MOTION_GUIDELINES.md`, `ACCESSIBILITY_GUIDE.md`, `VISUAL_DIRECTION.md`) — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 86 documents.
- **Résolution partielle du différé d'identité visuelle** : `docs/COLOR_SYSTEM.md`, `docs/TYPOGRAPHY_GUIDE.md`, `docs/LOGO_GUIDE.md` contiennent désormais des propositions concrètes (indigo `#5B4FE0` en accent principal, typographie General Sans recommandée, brief conceptuel de logo) — explicitement marquées « v1, à valider visuellement », pas des décisions finales. Voir la note dédiée ci-dessus, à ne jamais perdre en résumant ce journal.
- Consolidation : `MOTION_BRANDING.md`, `ACCESSIBILITY_VISUAL_GUIDE.md`, `ART_DIRECTION.md` demandés par ce cadrage recoupaient fortement des documents déjà écrits (Phase 1 vol. 3, Phase 2 vol. 1) — étendus en place plutôt que dupliqués.
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : validation visuelle humaine des trois propositions v1 (couleur, typographie, logo), les deux points de direction visuelle de la Phase 2 vol. 1 (chaleur du minimalisme, mode sombre par défaut), toutes les décisions produit/UX des phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP), reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### Phase 3 — Language System (2026-08-03)
- 9 nouveaux documents rédigés dans `docs/` (mécanique d'écriture, microcopy, dialogues, tooltips, notifications, onboarding, localisation, style/nommage, synthèse capstone) + extension de `docs/VOICE_AND_TONE.md` (§3bis ton par situation, §3ter comparaison de ton) — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 95 documents.
- Consolidation annoncée à l'utilisateur avant de commencer, confirmée à l'auto-revue : `VOICE_AND_TONE_GUIDE.md` (déjà couvert par `docs/VOICE_AND_TONE.md`), `GLOSSARY.md` (déjà couvert par `docs/VOCABULARY.md`), `ERROR_COPY_GUIDE.md`/`EMPTY_STATE_COPY.md` (déjà couverts par `docs/ERROR_STATES.md`/`docs/ERROR_EXPERIENCE.md` et `docs/EMPTY_STATES.md`/`docs/EMPTY_STATES_GUIDE.md`) n'ont pas été créés séparément. La section « NOMMAGE » du cadrage est repliée dans `docs/STYLE_GUIDE.md` §6, sans fichier dédié.
- `docs/LANGUAGE_SYSTEM.md` (capstone) introduit la cohérence cross-canal (application/site/documentation/e-mails) qui n'avait de foyer dans aucun document existant — le site web et les e-mails transactionnels restent des anticipations de posture, pas des spécifications de contenu (fonctionnalités non spécifiées à ce jour).
- Auto-revue effectuée : tous les `[[wikilinks]]` de `docs/` résolvent (seule occurrence non résolue, `[[Document.md]]` dans `docs/DOCUMENTATION_GUIDE.md`, est un placeholder de template intentionnel, pas une erreur) ; citations de sections vérifiées contre les titres réels (`PRODUCT_RULES.md` §7, `USER_JOURNEYS.md` §11, `VOCABULARY.md` §4, `PERSONALITY.md` §8, `UX_PRINCIPLES.md` §4, `VOICE_AND_TONE.md` §3bis) — aucune erreur trouvée cette fois.
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : langues cibles au-delà du français/anglais source non tranchées (`docs/LOCALIZATION_GUIDE.md` §6), toutes les décisions produit/UX/visuelles des phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP), reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

> **Correction de statut de push** : les mentions « non poussé sur `origin/main` » des entrées Phase 2 volume 1, Phase 2 volume 2 et Phase 3 ci-dessus sont désormais obsolètes — les trois phases ont été poussées ensemble sur demande explicite de l'utilisateur (commit `0ee8635`, 33 fichiers). Conservées telles quelles ci-dessus pour l'exactitude historique de chaque entrée au moment où elle a été écrite.

### Phase 4 — Premium Experience Bible (2026-08-03)
- 10 nouveaux documents rédigés dans `docs/` (bibliothèque de micro-interactions, chorégraphie du lecteur, bibliothèque d'animations nommées, transitions de page, squelettes de chargement, feedback visuel, thème dynamique, immersion, 110 détails premium, synthèse capstone) + extension de `docs/MOTION_GUIDELINES.md` (§12ter rythme/rebond/élasticité/orchestration) et `docs/SOUND_DESIGN_GUIDE.md` (§7 redondance accessibilité/haptique future) — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 105 documents.
- Consolidation annoncée à l'utilisateur avant de commencer, confirmée à l'auto-revue : `MOTION_SYSTEM.md` (le langage de mouvement demandé — durées, courbes, catégories — existait déjà en profondeur dans `docs/MOTION_GUIDELINES.md` depuis la Phase 1 volume 3, étendu en Phase 2 volume 2) et `SOUND_EXPERIENCE.md` (la même liste d'événements sonores avait déjà reçu une réponse explicite et volontairement minimale dans `docs/SOUND_DESIGN_GUIDE.md` en Phase 2 volume 2) n'ont pas été créés séparément — recréer un second document sur le même sujet aurait soit dupliqué, soit contredit silencieusement une décision déjà actée.
- Cette phase a nécessité une lecture préalable approfondie de 11 documents existants (`MOTION_GUIDELINES.md`, `INTERACTION_GUIDELINES.md`, `SOUND_DESIGN_GUIDE.md`, `THEMES_GUIDE.md`, `PLAYER_SPECIFICATION.md`, `DESIGN_TOKENS.md`, `ACCESSIBILITY_GUIDE.md`, `COLOR_SYSTEM.md`, `SURFACE_SYSTEM.md`, `ERROR_EXPERIENCE.md`, `EMPTY_STATES_GUIDE.md`) avant toute rédaction, pour que chaque nouveau document (`INTERACTION_LIBRARY.md`, `PLAYER_EXPERIENCE.md`, `DYNAMIC_THEME_GUIDE.md`, `VISUAL_FEEDBACK_GUIDE.md` en particulier) reste scopé étroitement en complément plutôt qu'en répétition de ce qui existait déjà.
- `docs/PREMIUM_DETAILS.md` documente 110 détails (objectif du cadrage : au moins 100), chacun avec une justification propre et un renvoi croisé plutôt qu'une redite d'une règle déjà établie.
- Auto-revue effectuée : tous les `[[wikilinks]]` de `docs/` résolvent (même exception connue `[[Document.md]]`, placeholder de template intentionnel) ; citations de sections vérifiées contre les titres réels (`SURFACE_SYSTEM.md` §2/§5/§7, `COLOR_SYSTEM.md` §6/§7, `THEMES_GUIDE.md` §5/§6/§9, `PLAYER_SPECIFICATION.md` §2 à §12, `ERROR_EXPERIENCE.md` §3/§5, `UX_PRINCIPLES.md` §1/§2, `DESIGN_TOKENS.md` §3/§5) — aucune erreur trouvée.
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Poussé sur `origin/main` (commit `1798369`, 14 fichiers).
- Ouvert : toutes les décisions produit/UX/visuelles/linguistiques des phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP) — les règles définies ici (durées, courbes, chorégraphies) devront être vérifiées en conditions réelles d'implémentation, pas seulement en revue documentaire ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### Phase 5 — Design System (2026-08-03)
- 4 nouveaux documents rédigés dans `docs/` (constitution/capstone, principes fondateurs, système d'espacement, patterns de composition de layout) + extension de `docs/DESIGN_TOKENS.md` (§6bis catégories breakpoint/container/grid, §6 gouvernance/versioning), `docs/LAYOUT_SYSTEM.md` (§3bis/§3ter grilles laptop/ultra-wide, §8 conteneurs), `docs/TYPOGRAPHY_GUIDE.md` (§4bis hiérarchie sémantique à 10 rôles), `docs/COLOR_SYSTEM.md` (§6bis tokens d'état hover/pressed/disabled/focus) et `docs/RESPONSIVE_GUIDE.md` (§7bis règle de priorité/masquage) — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 109 documents.
- Phase la plus consolidée à ce jour : sur les 14 livrables demandés, 3 (`DESIGN_TOKENS.md`, `COLOR_SYSTEM.md`, `LAYOUT_SYSTEM.md`) existaient déjà sous ce nom exact depuis la Phase 2 volume 2 et ont été étendus plutôt que réécrits ; 4 autres (`GRID_SYSTEM.md`, `TYPOGRAPHY_SYSTEM.md`, `RESPONSIVE_SYSTEM.md`) recoupaient des documents existants sous un autre nom et ont été traités par extension ; 4 (`ICON_SYSTEM.md`, `ACCESSIBILITY_SYSTEM.md`, `MOTION_TOKENS.md`, `ELEVATION_SYSTEM.md`) étaient déjà intégralement couverts par `docs/ICONOGRAPHY_GUIDE.md`/`docs/ACCESSIBILITY_GUIDE.md`/`docs/MOTION_GUIDELINES.md`/`docs/SURFACE_SYSTEM.md` sans qu'aucune extension ne soit nécessaire. Seuls 4 livrables (`DESIGN_SYSTEM.md`, `FOUNDATIONS.md`, `SPACING_SYSTEM.md`, `COMPOSING_RULES.md`) étaient réellement nouveaux.
- Contradiction explicitement signalée et tranchée, pas résolue en silence : l'exemple du cadrage (« jamais plus de deux niveaux d'élévation ») contredisait la décision déjà actée et justifiée de `docs/SURFACE_SYSTEM.md` §3 (cinq niveaux, 0 à 4) — tranché en faveur de la règle déjà actée, documenté dans `docs/DESIGN_SYSTEM.md` §2.
- `docs/DESIGN_SYSTEM.md` (capstone) introduit huit règles absolues vérifiables en revue de code, au même titre que `docs/DEFINITION_OF_DONE.md`.
- Auto-revue effectuée : tous les `[[wikilinks]]` de `docs/` résolvent (même exception connue `[[Document.md]]`, placeholder de template intentionnel) ; citations de sections vérifiées contre les titres réels (`DESIGN_TOKENS.md` §2/§4, `SURFACE_SYSTEM.md` §3, `TYPOGRAPHY_GUIDE.md` §4bis/§7, `COLOR_SYSTEM.md` §6bis, `RESPONSIVE_GUIDE.md` §5/§7bis, `LAYOUT_SYSTEM.md` §1-3ter/§6/§8, `DESIGN_SYSTEM_ARCHITECTURE.md` §1/§5/§6, `ICONOGRAPHY_GUIDE.md` §5, `FOUNDATIONS.md` §3/§4/§6/§7/§9) — aucune erreur trouvée.
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : toutes les décisions produit/UX/visuelles/linguistiques/sensorielles des phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP) — les règles absolues de `docs/DESIGN_SYSTEM.md` §3 devront être vérifiées en conditions réelles d'implémentation (revue de code, Storybook) pas seulement en revue documentaire ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### Phase 6 — Component Library (2026-08-04)
- 15 nouveaux documents rédigés dans `docs/` couvrant 135 composants nommés dans le cadrage — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 124 documents.
- **Stratégie de profondeur à deux niveaux annoncée à l'utilisateur avant de commencer** (`docs/COMPONENT_LIBRARY.md` §2) : spécification complète (13 sections) pour ~20 composants fondamentaux à plus fort réemploi (Button, Card, TextField, Dialog, Toast, Tabs, Sidebar, Grid, formes du lecteur, Queue Item, Search Results, Album Grid, Track Card, Preference Row, Empty State), spécification compacte avec renvoi systématique pour les ~110 autres — une profondeur égale partout aurait produit un volume ingérable et desservi l'objectif de référence officielle par sa propre exhaustivité.
- `Date Picker`, `Color Picker`, `Equalizer Panel` traités en architecture seulement (objectif anticipé + contrat d'interface attendu, sans les 13 sections), cohérent avec leur statut non engagé dans `docs/FEATURE_ROADMAP.md` et avec la demande explicite du cadrage lui-même pour ces trois composants.
- `docs/ACCESSIBILITY_COMPONENTS.md` introduit un contrat d'accessibilité transverse (focus, régions live, zones tactiles, patterns ARIA de référence) que chaque composant référence plutôt que de répéter — pour qu'une règle modifiée une fois se propage partout.
- Cette phase documente la couche composant réutilisable (anatomie, variantes, tokens, tests) au-dessus de deux couches déjà écrites : les spécifications produit (Phases 1-2 : `PLAYER_SPECIFICATION.md`, `SEARCH_SPECIFICATION.md`, `LIBRARY_SPECIFICATION.md`, `SETTINGS_SPECIFICATION.md`, `ERROR_EXPERIENCE.md`, `EMPTY_STATES_GUIDE.md`) et le Design System (Phase 5) — aucun comportement produit n'est redécidé, uniquement référencé.
- `docs/COMPONENT_CHECKLIST.md` couvre le bonus demandé par le cadrage (matrice de composition, paires jamais imbriquées, tokens partagés, comportements hérités, tableau de dépendances d'implémentation) — non scindé en fichier séparé faute de nom demandé pour ce livrable.
- Auto-revue initiale (structurelle) : tous les `[[wikilinks]]` de `docs/` résolvent (même exception connue `[[Document.md]]`, placeholder de template intentionnel) ; citations de sections vérifiées contre les titres réels (`ERROR_EXPERIENCE.md` §2/§3/§5, `EMPTY_STATES_GUIDE.md` §1/§2/§3/§4/§5, `SKELETON_SYSTEM.md` §2/§4, `PLAYER_SPECIFICATION.md` §2/§3/§6/§7/§11, `DIALOG_LIBRARY.md` §5) — cette passe n'avait vérifié que la structure (liens/citations), pas le fond.
- **Relecture intégrale demandée explicitement par l'utilisateur (2026-08-04), plus approfondie que l'auto-revue initiale** — a trouvé et corrigé des erreurs réelles que la passe structurelle n'avait pas révélées :
  - **Décompte erroné** : le total annoncé (« 134 composants ») était faux — un recomptage précis par catégorie donne 47 en Foundation (pas 46), soit 135 au total. Corrigé dans `docs/COMPONENT_LIBRARY.md`, `docs/DOCUMENTATION_GUIDE.md` et ce journal.
  - **Contradiction réelle** : `Notification` était défini dans `docs/FEEDBACK_COMPONENTS.md` comme un concept purement système (hors application), alors que `docs/ACCESSIBILITY_COMPONENTS.md` §3/§6 le traite comme un composant in-app à `aria-live`, aux côtés de Toast/Snackbar/Banner/Alert. Redéfini comme variante de Toast pour événement de fond, avec équivalent système décrit séparément.
  - **Incohérence préexistante (Phase 2) mise au jour par la Phase 6** : `docs/SURFACE_SYSTEM.md` §6 affirme depuis la Phase 2 que Tooltip est « toujours au-dessus de tout le reste », mais l'échelle de z-index de `docs/DESIGN_TOKENS.md` §3 ne comportait aucun niveau `tooltip` et présentait `toast` comme jamais recouvert — les deux ne pouvaient pas être vrais simultanément. Résolu en ajoutant un niveau `tooltip` (60) au-dessus de `toast` (50) dans `docs/DESIGN_TOKENS.md` §3.
  - **Composants manquants** : `Album Card`, `Artist Card`, `Playlist Card`, `Genre Card`, `Library Card` étaient promis par l'index (`docs/COMPONENT_LIBRARY.md` §4) mais jamais réellement écrits dans `docs/LIBRARY_COMPONENTS.md` — seuls leurs Grids conteneurs l'étaient. Ajoutés comme entrées explicites, chacune dérivant de `docs/CARD_SPECIFICATION.md`.
  - **Section sous-livrée par rapport au cadrage** : la comparaison de qualité demandée explicitement (Material Design 3, Fluent UI, Carbon Design System, Polaris, Radix UI, shadcn/ui, Apple HIG) ne couvrait que 3 des 7 références nommées, et pointait vers un renvoi cassé (`docs/COMPONENT_CHECKLIST.md` §5, qui est en réalité la table d'héritage, pas une auto-revue). `docs/COMPONENT_LIBRARY.md` §5 réécrit pour couvrir les 7 références.
  - Corrections mineures de citations (`BUTTON_SPECIFICATION.md`, `FORM_COMPONENTS.md`, `SEARCH_COMPONENTS.md`, `COMPONENT_CHECKLIST.md`) : styles de renvoi incohérents, citation imprécise, pseudo-composant inexistant (« VisualFeedback »), nom incohérent (« Search Empty State » vs « Empty Search State »), fusion SearchField/Global Search rendue explicite plutôt qu'implicite.
- **Cette documentation est désormais considérée comme la référence officielle du projet**, conformément à la demande explicite de l'utilisateur — toute contradiction future avec elle doit être signalée explicitement plutôt que résolue en silence, cohérent avec `docs/DOCUMENTATION_GUIDE.md` §5.
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : toutes les décisions produit/UX/visuelles/linguistiques/sensorielles/architecturales des phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP) — les ~110 composants en profondeur compacte devront être approfondis au fil de l'implémentation réelle plutôt qu'en un seul passage documentaire supplémentaire ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### Phase 7 — Foundation Components (2026-08-04)
- 5 nouveaux documents rédigés dans `docs/` (capstone, atomes d'affichage, hiérarchie officielle, graphe de dépendances fin, guide de test) + extension de `docs/BUTTON_SPECIFICATION.md`, `docs/FORM_COMPONENTS.md`, `docs/FEEDBACK_COMPONENTS.md`, `docs/NAVIGATION_COMPONENTS.md` et `docs/COMPONENT_CHECKLIST.md` — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 129 documents.
- **Collision de nom la plus étendue du projet à ce jour** : 5 des 10 livrables demandés (`BUTTON_SPECIFICATION.md`, `FORM_COMPONENTS.md`, `FEEDBACK_COMPONENTS.md`, `NAVIGATION_COMPONENTS.md`, `COMPONENT_CHECKLIST.md`) existaient déjà sous ce nom exact depuis la Phase 6, avec un gabarit à 13 sections quasi identique (« Motion » remplaçant « Animations », le reste inchangé) — tous étendus avec les composants réellement nouveaux plutôt que réécrits.
- **Sept fusions explicites** (même pattern que SearchField/Global Search en Phase 6, jamais un doublon silencieux) : Search Bar=SearchField, Autocomplete=Combobox, Confirmation Dialog=Dialog (variante Destructive), Empty Placeholder=Empty State, Segmented Control=SegmentedButton, Navigation Rail=Sidebar (variante Réduite), Command Menu=Command Palette — documentées dans chaque fichier concerné et récapitulées dans `docs/FOUNDATION_COMPONENTS.md` §1.
- `docs/DISPLAY_COMPONENTS.md` ne redéfinit pas Badge/Chip/Tag/Avatar/Divider/Separator (déjà spécifiés en Phase 6) — uniquement les atomes réellement nouveaux (User Avatar, Artwork, Album Cover, Thumbnail, Label, Caption, Code Block), avec Label/Caption explicitement identifiés comme rôles typographiques et non comme composants à variantes/états propres.
- `docs/COMPONENT_HIERARCHY.md` et `docs/COMPONENT_DEPENDENCY_GRAPH.md` répondent chacun à une question distincte du bonus déjà couvert en Phase 6 par `docs/COMPONENT_CHECKLIST.md` §2-6 (composition/héritage/dépendances par palier) : classification par nature (fondamental/dérivé/composé/domaine) pour le premier, arêtes de dépendance nommées et composants à plus fort impact en cascade pour le second — aucun des trois documents ne redécide ce que les deux autres couvrent déjà.
- `docs/FOUNDATION_TESTING_GUIDE.md` ne redécide ni la pyramide de tests ni les seuils de couverture (déjà actés dans `docs/TESTING_STRATEGY.md` depuis la Phase 0.5) — applique uniquement §3/§7 de ce document à la couche fondamentale.
- Auto-revue effectuée : tous les `[[wikilinks]]` de `docs/` résolvent (même exception connue `[[Document.md]]`) ; citations vérifiées.
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : toutes les décisions produit/UX/visuelles/linguistiques/sensorielles/architecturales des phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP) ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### Phase 8 — Navigation System (2026-08-04)
- 9 nouveaux documents rédigés dans `docs/` (constitution/capstone, spécification complète de la TopBar, approfondissement de la Command Palette, navigation à travers la recherche, historique de navigation, bibliothèque de raccourcis, architecture mobile, matrice/diagramme de parcours, checklist de validation) + extension de `docs/LAYOUT_COMPONENTS.md`, `docs/RESPONSIVE_GUIDE.md`, `docs/ACCESSIBILITY_GUIDE.md`, `docs/ANIMATION_LIBRARY.md`, `docs/INTERACTION_GUIDELINES.md`, `docs/OVERLAY_COMPONENTS.md` et `docs/NAVIGATION_COMPONENTS.md` — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 138 documents.
- Consolidation annoncée avant de commencer, confirmée à l'auto-revue : `SIDEBAR_SPECIFICATION.md` (Sidebar avait déjà une spécification complète à 13 sections depuis la Phase 6, étendue avec les variantes Pinned/Floating/Docked/Auto Hide/Adaptive Width plutôt que dupliquée), `RESPONSIVE_NAVIGATION.md` (`docs/RESPONSIVE_GUIDE.md` couvrait déjà le comportement de navigation par classe d'appareil, complété par une préparation TV/manette honorant un renvoi posé depuis la Phase 1), `NAVIGATION_ACCESSIBILITY.md` (`docs/ACCESSIBILITY_GUIDE.md`/`docs/ACCESSIBILITY_COMPONENTS.md` couvrent déjà l'accessibilité transverse, complétés par les liens d'évitement et régions de repère propres à la navigation) et `NAVIGATION_ANIMATIONS.md` (`docs/TRANSITION_GUIDE.md`/`docs/ANIMATION_LIBRARY.md` couvrent déjà le mouvement, complétés par les deux animations manquantes — Menu/Popover et Command Palette — qui n'avaient encore aucune entrée nommée) n'ont pas été créés séparément.
- **Conflit réel trouvé et corrigé en auto-revue, pas seulement une incohérence documentaire** : `docs/INTERACTION_GUIDELINES.md` §1 assignait `Ctrl/Cmd + Q` à « Ajouter à la file d'attente » depuis la Phase 1 — or `Cmd + Q` est le raccourci système réservé pour Quitter l'application sur macOS, intercepté au niveau OS et non remappable dans une application native/Tauri. Corrigé en `Ctrl/Cmd + Shift + Q`, documenté dans `docs/KEYBOARD_SHORTCUTS.md` §11 (combinaisons réservées, jamais assignables) pour que ce type d'erreur ne se reproduise pas sur un futur raccourci.
- `docs/NAVIGATION_CHECKLIST.md` se distingue explicitement de `docs/COMPONENT_CHECKLIST.md` §1 : le second valide un composant isolé, le premier valide un parcours complet à travers plusieurs composants et méthodes d'entrée — les deux sont complémentaires, jamais substituables.
- Auto-revue effectuée : tous les `[[wikilinks]]` de `docs/` résolvent (même exception connue `[[Document.md]]`) ; citations vérifiées, dont `EVOLVABILITY.md` §3-4 (Android TV/Apple TV) et `PLAYER_EXPERIENCE.md` §9 (file d'attente).
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : toutes les décisions produit/UX/visuelles/linguistiques/sensorielles/architecturales des phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP) — la préparation TV/manette (`docs/RESPONSIVE_GUIDE.md` §7ter) reste une architecture non activée, jamais une fonctionnalité livrée ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### Phase 9 — Music Component Library (2026-08-04)
- 12 nouveaux documents rédigés dans `docs/` (constitution/capstone, composants Album/Artiste/Track/Playlist/Collection, système de paroles, visualiseur audio, système de pochette, appareils de lecture, téléchargement, hors ligne) + extension de `docs/PLAYER_COMPONENTS.md`, `docs/PLAYER_SPECIFICATION.md`, `docs/QUEUE_SPECIFICATION.md`, `docs/DYNAMIC_THEME_GUIDE.md`, `docs/LIBRARY_COMPONENTS.md`, `docs/SEARCH_SPECIFICATION.md`, `docs/ACCESSIBILITY_GUIDE.md`, `docs/FOUNDATION_TESTING_GUIDE.md` et `docs/PERFORMANCE_GUIDE.md` — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 150 documents.
- **Consolidation la plus large de ce projet en proportion** : sur les 22 livrables demandés, 9 (`PLAYER_SYSTEM.md`, `PLAYBACK_CONTROLS.md`, `QUEUE_SYSTEM.md`, `DYNAMIC_THEME_ENGINE.md`, `STATISTICS_COMPONENTS.md`, `WRAPPED_COMPONENTS.md`, `SEARCH_MUSIC.md`, `MUSIC_ACCESSIBILITY.md`, `MUSIC_TESTING_GUIDE.md`, `MUSIC_PERFORMANCE_GUIDE.md` — en réalité 10 noms, `MUSIC_PERFORMANCE_GUIDE.md` inclus) recoupaient des documents déjà profonds des Phases 1 (`PLAYER_SPECIFICATION.md`, `QUEUE_SPECIFICATION.md`, `STATISTICS_SPECIFICATION.md`, `WRAPPED_SPECIFICATION.md`, `SEARCH_SPECIFICATION.md`), 4 (`DYNAMIC_THEME_GUIDE.md`), 6 (`PLAYER_COMPONENTS.md`, `LIBRARY_COMPONENTS.md`), 7 (`FOUNDATION_TESTING_GUIDE.md`) et 8 — étendus avec les sous-fonctionnalités réellement nouvelles plutôt que dupliqués.
- Trois extensions notables : `docs/PLAYER_SPECIFICATION.md` §5bis introduit le Sleep Timer (réellement nouveau, jamais couvert) ; `docs/QUEUE_SPECIFICATION.md` §6bis-6quater ajoute filtres/recherche dans la file, sauvegarde explicite en playlist et une contrainte de sécurité pour une future synchronisation multi-appareils ; `docs/DYNAMIC_THEME_GUIDE.md` §5bis répond explicitement à l'exigence « limiter les variations, préserver l'identité visuelle » restée implicite jusqu'ici.
- `docs/TRACK_COMPONENTS.md` introduit **Track Row**, un troisième contexte de représentation d'un titre (corps d'Album/Playlist) explicitement distingué de Track Card ([[LIBRARY_COMPONENTS.md]], grille) et Queue Item ([[PLAYER_COMPONENTS.md]], file) — trois composants pour trois contextes, jamais un seul composant surchargé de conditions.
- `docs/COLLECTION_COMPONENTS.md` §3 clarifie qu'un seul mécanisme de favori existe dans toute l'application — la vue Favorites est une lecture filtrée de ce statut unique, jamais un second système parallèle.
- `docs/ARTWORK_SYSTEM.md` comble un vide identifié entre `docs/DISPLAY_COMPONENTS.md` (Phase 7, une ligne par composant image) et `docs/DYNAMIC_THEME_GUIDE.md` (Phase 4, l'extraction et le dégradé) — le traitement systématique de l'image elle-même (repli, chargement, flou, ombre, glow) n'avait de foyer dans aucun document existant.
- Auto-revue effectuée : tous les `[[wikilinks]]` de `docs/` résolvent (même exception connue `[[Document.md]]`) ; citations vérifiées, dont `ARCHITECTURE_PRINCIPLES.md` §3.3 (stratégie de synchronisation, déjà « dernier écrit gagne » — cohérent avec la contrainte ajoutée dans `docs/OFFLINE_SYSTEM.md` §6) et `docs/FEATURE_BIBLE.md` §5 (playlists collaboratives, statut encore ouvert).
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : toutes les décisions produit/UX/visuelles/linguistiques/sensorielles/architecturales/de navigation des phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP) — AirPlay/Chromecast/Multiroom (`docs/PLAYBACK_DEVICES.md`) et Picture in Picture (`docs/PLAYER_COMPONENTS.md`) restent des préparations architecturales non activées ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### Phase 10 — Screen System (2026-08-04)
- 16 nouveaux documents rédigés dans `docs/` (constitution/capstone, écrans Accueil/Bibliothèque/Album/Artiste/Playlist/Lecteur/Recherche/Téléchargements/Statistiques/Paramètres/Synchronisation/Erreurs/Onboarding, synthèse responsive cross-écran, matrice composants/écrans) + extension de `docs/TRANSITION_GUIDE.md` et `docs/PERFORMANCE_GUIDE.md` — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 166 documents.
- **Principe directeur explicite de cette phase** (posé par le cadrage lui-même) : les composants sont désormais figés (Phases 5-9) — chaque document ne documente que leur assemblage (quelle zone, quel composant, quel ordre), jamais un comportement déjà spécifié. `docs/SCREEN_SYSTEM.md` §2-3 formalise un gabarit d'écran délibérément plus compact que le gabarit à 13 sections des composants, pour cette raison précise.
- `SCREEN_TRANSITIONS.md` et `SCREEN_PERFORMANCE_GUIDE.md` demandés par ce cadrage n'ont pas été créés séparément — recoupaient `docs/TRANSITION_GUIDE.md` (Phase 4, 9 transitions déjà nommées) et `docs/PERFORMANCE_GUIDE.md`, étendus plutôt que dupliqués.
- Plusieurs fusions terminologiques explicites documentées plutôt que des écrans dupliqués : Authentication fusionnée dans l'écran Server Connection (`docs/ONBOARDING_SCREENS.md`), Achievements = Badges déjà spécifiés (`docs/STATISTICS_SCREENS.md`), Connection Lost traité comme bannière et non comme écran plein (`docs/ERROR_SCREENS.md`).
- `docs/ERROR_SCREENS.md` §9 tranche explicitement que seuls 2 des 7 cas d'erreur nommés par le cadrage (Server Unreachable, Corrupted Cache) justifient un écran plein — les 5 autres restent des patterns bannière/toast/snackbar/dialog déjà définis, appliqués à un contexte précis plutôt que redécidés comme de nouveaux écrans, cohérent avec la règle absolue déjà actée dans `docs/ERROR_EXPERIENCE.md` §3.
- `docs/RESPONSIVE_LAYOUTS.md` synthétise par région (Header/Sidebar/Hero/Main/Right Panel) plutôt que par écran individuel — chaque document d'écran y renvoie plutôt que de répéter la même table de classes d'appareil.
- Auto-revue effectuée : tous les `[[wikilinks]]` de `docs/` résolvent (même exception connue `[[Document.md]]`) ; citations vérifiées, dont `docs/SCREEN_SPECIFICATIONS.md` §6 (indicateur de réglage non par défaut, avertissement Labs).
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : toutes les décisions produit/UX/visuelles/linguistiques/sensorielles/architecturales/de navigation des phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP) — cette phase complète la documentation de conception ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### Phase 11 — System Experience Framework (2026-08-04)
- 12 nouveaux documents rédigés dans `docs/` (constitution/capstone, référentiel exhaustif des réglages, moteur de synchronisation, système de cache, diagnostics, maintenance, mises à jour, import/export, logs, feature flags, matrice bonus, checklist système) + extension de `docs/DOWNLOAD_SYSTEM.md`, `docs/OFFLINE_SYSTEM.md` (Phase 9), `docs/NOTIFICATION_LIBRARY.md`, `docs/ERROR_STATES.md` et `docs/FOUNDATION_TESTING_GUIDE.md` — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 178 documents.
- **Deux collisions de nom exactes** : `DOWNLOAD_SYSTEM.md` et `OFFLINE_SYSTEM.md`, demandés à nouveau par ce cadrage, existaient déjà sous ce nom précis depuis la Phase 9 — étendus (reprise après redémarrage et téléchargement intelligent pour le premier, mode avion pour le second) plutôt que réécrits, jamais un doublon de fichier avec un contenu différent sous un même nom.
- `NOTIFICATION_SYSTEM.md`, `ERROR_SYSTEM.md` et `SYSTEM_TESTING_GUIDE.md` recoupaient `docs/NOTIFICATION_LIBRARY.md`, `docs/ERROR_STATES.md` et `docs/FOUNDATION_TESTING_GUIDE.md` — également étendus plutôt que dupliqués.
- **Mise à jour d'un statut devenu obsolète, trouvée en auto-revue** : `docs/ERROR_STATES.md` §9 affirmait depuis la Phase 1 qu'aucun comportement de résolution de conflit n'était défini — or `docs/OFFLINE_SYSTEM.md` §5-6 (Phase 9) avait depuis posé le comportement de surface (jamais de perte silencieuse, jamais de blocage bloquant). Corrigé pour refléter l'état réel : la stratégie technique précise reste ouverte (ADR non rédigé), mais le comportement de surface est désormais défini et référencé.
- `docs/LOGGING_SYSTEM.md` devient la destination commune de tout ce qui journalise ailleurs dans le projet (`docs/ERROR_STATES.md` §9bis, `docs/SYNC_ENGINE_SPECIFICATION.md` §6) — jamais un système de collecte parallèle par document.
- `docs/SYSTEM_CHECKLIST.md` se distingue explicitement de `docs/COMPONENT_CHECKLIST.md` (composant isolé) et `docs/NAVIGATION_CHECKLIST.md` (parcours de navigation) : il valide une fonctionnalité système qui opère souvent en arrière-plan, où l'échec silencieux serait particulièrement coûteux pour la confiance de l'utilisateur.
- Auto-revue effectuée : tous les `[[wikilinks]]` de `docs/` résolvent (même exception connue `[[Document.md]]`) ; citations vérifiées, dont `docs/JELLYFIN_INTEGRATION.md` §3 (synchronisation incrémentale déjà actée en Phase 0.5, confirmée cohérente avec `docs/SYNC_ENGINE_SPECIFICATION.md` sans contradiction).
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : toutes les décisions produit/UX/visuelles/linguistiques/sensorielles/architecturales/de navigation/système des phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP) — Rollback (`docs/UPDATE_SYSTEM.md` §5), canal bêta (`docs/FEATURE_FLAGS.md` §4) et ReplayGain (`docs/SETTINGS_SYSTEM.md` §3) restent des préparations non activées ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### Phase 12 — Software Architecture (2026-08-04)
- 4 nouveaux documents rédigés dans `docs/` (registre des 18 modules de fonctionnalité, flux de données de bout en bout DTO→Mapper→Entité→Repository→Cache→Store→ViewModel→UI, gestion des erreurs au niveau code, guide de configuration d'environnement) + extension de `docs/ARCHITECTURE.md` (§3bis matrice de dépendance au niveau module), `docs/CODING_STANDARDS.md` (§1bis dossiers complémentaires + nommage), `docs/ARCHITECTURE_PRINCIPLES.md` (§8bis glossaire SOLID/KISS/YAGNI/Clean Architecture/Ports & Adapters/DI/Repository Pattern), `docs/PERFORMANCE_GUIDE.md` (§5bis mémoïsation/tree shaking/workers), `docs/SECURITY_GUIDE.md` (§3bis gestion du jeton Jellyfin), `docs/ACCESSIBILITY_GUIDE.md` (§9ter outillage de développement) et `docs/TECHNICAL_BLUEPRINT.md` (§5bis auto-revue comparative) — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 182 documents.
- **Phase la plus consolidation-lourde du projet à ce jour, par nature du cadrage lui-même** : sur les 13 livrables demandés, seuls 4 (`MODULES.md`, `DATA_FLOW.md`, `ERROR_HANDLING.md`, `CONFIGURATION_GUIDE.md`) étaient réellement nouveaux — les 9 autres (`ARCHITECTURE.md`, `DEPENDENCY_RULES.md`, `NAMING_CONVENTIONS.md`, `PERFORMANCE_GUIDE.md`, `SECURITY_GUIDE.md`, `ACCESSIBILITY_GUIDE.md`, `ENGINEERING_STANDARDS.md`, `SOFTWARE_PRINCIPLES.md`) redemandaient en profondeur ce que les Phases 0 et 0.5 avaient déjà construit dans le détail (couches UI/Domain/Data/Platform, interfaces `MusicSource`/`LocalStore`, monorepo à frontières de packages réelles, budgets de performance, CSP/scan de dépendances, patterns ARIA) — chacun vérifié puis étendu au bon endroit, jamais réécrit ou dupliqué sous un nom voisin.
- `docs/ENGINEERING_STANDARDS.md` en particulier n'a reçu strictement aucun contenu propre : entièrement redirigé vers `docs/ENGINEERING_GUIDE.md`/`docs/CODING_STANDARDS.md` déjà exhaustifs sur ce sujet précis — plutôt qu'un fichier vide ou un doublon partiel, ce livrable n'existe pas comme fichier séparé, décision annoncée explicitement ici comme dans les phases précédentes.
- `docs/ARCHITECTURE_PRINCIPLES.md` §8bis introduit un glossaire qui nomme SOLID/KISS/YAGNI/Clean Architecture/Ports & Adapters/DI/Repository Pattern par leur terme classique — aucun de ces principes n'était redécidé, chacun était déjà appliqué concrètement depuis la Phase 0 (ex. `MusicSource`/`LocalStore` = Ports & Adapters) sans être nommé ainsi jusqu'ici.
- `docs/DATA_FLOW.md` introduit trois formes typées (DTO, Mapper, ViewModel) jamais nommées explicitement ailleurs dans `docs/` malgré un mapping Jellyfin→Domain déjà acté depuis la Phase 0 (`docs/ARCHITECTURE_PRINCIPLES.md` §2.2) — ce document nomme et enchaîne le pipeline complet sans redécider l'abstraction `MusicSource`/`LocalStore` elle-même.
- `docs/ERROR_HANDLING.md` cadré strictement au niveau code (Error Boundaries, `Result<T,E>`, retry/backoff, journalisation) — ne redéfinit ni les messages utilisateur (`docs/ERROR_STATES.md`) ni les patterns visuels (`docs/ERROR_EXPERIENCE.md`), déjà propriétaires de ces sujets depuis la Phase 1.
- `docs/TECHNICAL_BLUEPRINT.md` §5bis compare l'architecture logicielle actée à six produits nommés (VS Code, Spotify, Raycast, Linear, Nextcloud Desktop, Plexamp), avec le même avertissement d'honnêteté que les analyses concurrentielles précédentes (connaissance du modèle, pas audit de code source en direct) — conclusion : aucun écart de fond trouvé, un seul écart assumé et déjà documenté (stratégie de synchronisation *pull* plutôt que temps réel bidirectionnelle façon Linear, ADR encore ouvert).
- Auto-revue effectuée : tous les `[[wikilinks]]` de `docs/` résolvent (même exception connue `[[Document.md]]`, placeholder de template intentionnel) ; citations vérifiées et une corrigée en cours de rédaction (`docs/LOGGING_SYSTEM.md` §3→§1, catégorie « Logs développeur » plus précise pour la journalisation d'un Error Boundary).
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : toutes les décisions produit/UX/visuelles/linguistiques/sensorielles/architecturales/de navigation/système des phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP) — cette phase achève la documentation d'architecture logicielle frontend ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### Phase 13 — Data Layer (2026-08-04)

> Un cadrage identique à celui de la Phase 12 (même équipe, mêmes 13 livrables) a été soumis juste avant celui-ci — signalé explicitement à l'utilisateur avant toute action, confirmé comme doublon accidentel, aucune action prise (voir l'échange correspondant, pas une entrée de phase distincte).

- 9 nouveaux documents rédigés dans `docs/` (spécification des DTO, entités de domaine officielles, guide de mapping à quatre directions, pattern repository par domaine avec matrice de dépendance, schéma logique complet de la base locale, implémentation Dexie concrète, moteur de statistiques, moteur de recommandation, moteur de playlists) + extension de `docs/JELLYFIN_INTEGRATION.md` (§7bis), `docs/CACHE_SYSTEM.md` (§1-2), `docs/SYNC_ENGINE_SPECIFICATION.md` (§1, §7bis-ter), `docs/DATA_LAYER.md` (§3.4 + §3bis capstone), `docs/DOWNLOAD_SYSTEM.md` (§5quater), `docs/LOGGING_SYSTEM.md` (§1), `docs/SECURITY_GUIDE.md` (§3ter), `docs/PERFORMANCE_GUIDE.md` (§6quater) et `docs/TESTING_STRATEGY.md` (§9bis) — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 191 documents.
- **12 des 21 livrables demandés n'ont pas donné lieu à un fichier séparé** : `API_CLIENT.md`, `CACHE_ENGINE.md`, `SYNC_ENGINE.md`, `IMPORT_ENGINE.md`, `SEARCH_INDEX_ENGINE.md`, `OFFLINE_ENGINE.md`, `DOWNLOAD_ENGINE.md`, `LOGGING_GUIDE.md`, `DATA_SECURITY.md`, `DATA_PERFORMANCE.md`, `DATA_TESTING_GUIDE.md` recoupaient chacun un document déjà profond des Phases 0.5/9/11/12, étendus plutôt que dupliqués ; `DATA_LAYER.md` lui-même, également demandé, existait déjà depuis la Phase 0.5 et devient le capstone de cette phase plutôt que d'être réécrit. Une vérification explicite d'`OFFLINE_SYSTEM.md` et de `LOGGING_SYSTEM.md` a été faite avant d'étendre — le premier couvrait déjà intégralement les cinq comportements demandés (aucune modification), le second avait un gap réel mineur (catégorie « Logs téléchargement » manquante), comblé sans réinventer le reste.
- **Gap réel identifié et comblé, pas seulement une consolidation de nom** : `docs/SECURITY_GUIDE.md` §3bis (Phase 12) avait déjà tranché le chiffrement du jeton d'authentification, mais aucune décision n'existait sur le chiffrement au repos du reste de `LocalStore` (métadonnées, historique d'écoute, favoris, playlists). Décision prise et justifiée en §3ter : pas de chiffrement systématique de l'ensemble (coût de performance disproportionné au modèle de menace visé — accès physique à l'appareil personnel de l'utilisateur, pas exfiltration réseau), avec une exception assumée documentée pour l'historique d'écoute (protection par contrôle utilisateur — accès/export/suppression — plutôt que par chiffrement) et un point de réévaluation explicite si une fonctionnalité multi-utilisateurs sur appareil partagé était introduite un jour.
- Trois nouveaux documents introduisent une distinction qui n'existait dans aucun document précédent : `docs/STATISTICS_ENGINE.md`, `docs/RECOMMENDATION_ENGINE.md` et `docs/PLAYLIST_ENGINE.md` séparent explicitement le **comportement produit** (déjà spécifié en Phase 1 dans `STATISTICS_SPECIFICATION.md`/`DISCOVERY_SPECIFICATION.md`/`PLAYLIST_SPECIFICATION.md`, non redécidé) de l'**architecture de calcul** (Web Worker, cache de résultat, interface de scoring, moteur de règles partagé) — un cadrage strict rappelé en tête de chacun des trois documents pour empêcher toute dérive future qui redéciderait le produit dans le mauvais document.
- `docs/DATABASE_SCHEMA.md` et `docs/INDEXEDDB_ARCHITECTURE.md` séparés délibérément : le premier est le schéma logique multiplateforme (valable pour `SqliteStore` et `IndexedDbStore`), le second l'implémentation Dexie concrète propre au repli Web — la frontière déjà actée en Phase 0.5 entre schéma logique et détail de plateforme (`docs/ARCHITECTURE_PRINCIPLES.md` §3.1) est ainsi respectée plutôt que mélangée dans un seul document.
- Correction en cours de rédaction : une citation erronée vers `PLAYER_SPECIFICATION.md` §3 (états de lecture) au lieu de `STATISTICS_SPECIFICATION.md` §2 (seuil de comptage d'une écoute) trouvée et corrigée dans `docs/REPOSITORY_PATTERN.md` avant la fin de l'auto-revue.
- Auto-revue effectuée : tous les `[[wikilinks]]` de `docs/` résolvent (même exception connue `[[Document.md]]`, placeholder de template intentionnel) ; citations vérifiées, dont la correction ci-dessus.
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : toutes les décisions produit/UX/visuelles/linguistiques/sensorielles/architecturales/de navigation/système des phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP) — cette phase achève la documentation de la couche donnée, la fréquence exacte de réévaluation des playlists/recommandations reste un point ouvert de conception détaillée (`docs/FEATURE_BIBLE.md` §5, non tranché arbitrairement dans `docs/RECOMMENDATION_ENGINE.md` §3) ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### Revue de consolidation documentaire (2026-08-04)

> Demandée explicitement avant l'entrée en Phase 1 d'ingénierie (MVP) — pas une nouvelle « phase » de contenu au sens des entrées précédentes de ce journal, mais un audit transverse des 191 documents déjà écrits. Nommée ainsi plutôt que « Phase 14 » pour ne pas laisser croire qu'il s'agit d'un contenu produit/technique nouveau.

- **Filet de sécurité posé avant toute action destructive** : tout le travail des Phases 5 à 13 (112 fichiers) n'avait encore jamais été commité — seules les Phases 0 à 4 étaient dans l'historique Git (commit `1798369`). Un commit local de checkpoint (`3023a4c`, non poussé) a été créé avant de commencer l'audit, sur confirmation explicite de l'utilisateur.
- **Constat central de l'audit, honnête plutôt que forcé** : la détection de doublons/contradictions (Étape 2 du cadrage) a trouvé très peu de problèmes réels à corriger. Quatre clusters à risque vérifiés manuellement (le cluster erreur à 4 documents `ERROR_STATES.md`/`ERROR_EXPERIENCE.md`/`ERROR_SCREENS.md`/`ERROR_HANDLING.md`, `THEMES_GUIDE.md` vs `DYNAMIC_THEME_GUIDE.md`, `NAVIGATION_GUIDE.md` vs `NAVIGATION_SYSTEM.md` vs `NAVIGATION_PATTERNS.md`, et la terminologie « Playback ») — chacun confirmé comme un exemple correct de séparation de responsabilités avec cadrage explicite, aucun comme un doublon réel. **Zéro fusion, zéro suppression, zéro renommage exécuté** — forcer des fusions inexistantes aurait contredit l'objectif même de la revue (documentation maintenable, pas réduction du nombre de fichiers pour la forme).
- Cette absence d'action destructive n'est pas un signe d'audit superficiel mais la conséquence directe de la discipline de consolidation déjà appliquée en continu depuis la Phase 6 sur chacune des 8 phases suivantes (annonce systématique des décisions de consolidation avant exécution, auto-revue de fin de phase, corrections en temps réel — voir les entrées Phase 6/8/11 de ce journal pour les erreurs réelles déjà trouvées et corrigées à l'époque).
- **6 nouveaux documents méta créés** plutôt qu'une réorganisation destructive : `docs/TABLE_OF_CONTENTS.md` (table des matières par domaine, 16 domaines, complète la carte chronologique déjà existante de `docs/DOCUMENTATION_GUIDE.md` §1 sans la dupliquer), `docs/DOCUMENT_HIERARCHY.md` (hiérarchie à 11 niveaux Vision→...→Couche donnée, avec une honnêteté explicite que les niveaux Implémentation/Tests/Déploiement restent aspirationnels — aucun code applicatif n'existe), `docs/DOCUMENT_DEPENDENCY_GRAPH.md` (documents à plus fort impact en cascade — `docs/ARCHITECTURE_PRINCIPLES.md` en tête avec 153 références entrantes — et matrice de dépendance par domaine), `docs/GLOSSARY.md` (glossaire technique/architectural, explicitement distinct de `docs/VOCABULARY.md` qui reste le glossaire de copy utilisateur), `docs/DOCUMENTATION_CHECKLIST.md` (formalise la RÈGLE ABSOLUE déjà pratiquée informellement : vérifier qu'un document similaire n'existe pas avant d'en créer un nouveau), `docs/ARCHITECTURE_REVIEW.md` (le rapport complet de cet audit).
- Vérification quantitative de l'architecture référentielle du corpus, jamais mesurée globalement auparavant : 4 820 occurrences de wikilinks sur 191 documents (25,2 liens sortants en moyenne), 0 document sans référence entrante, 0 document sans référence sortante, 0 lien cassé (hors le placeholder `[[Document.md]]` déjà connu depuis la Phase 3). Aucun document en dessous d'un seuil de « stub » (51 à 394 lignes sur l'ensemble du corpus) — aucune fusion ni découpage recommandé pour raison de densité.
- Aucun code applicatif écrit — voulu, cette revue reste documentaire.
- Non poussé sur `origin/main` à ce stade (le commit de checkpoint non plus).
- Ouvert : toutes les décisions produit/UX/visuelles/linguistiques/sensorielles/architecturales/de navigation/système des phases précédentes restent ouvertes, cette revue ne les tranche pas — entrée effective en Phase 1 d'ingénierie (MVP) désormais la seule étape restante avant que du code applicatif existe ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### Architecture d'état (2026-08-04)

- 10 nouveaux documents rédigés dans `docs/` (constitution capstone, spécification des stores, matrice de dépendance des stores, classification serveur/local/UI/dérivé, guide TanStack Query, système d'événements, guide des sélecteurs) + extension de `docs/TECHNOLOGY_COMPARISONS.md` (§3bis-3ter), `docs/PERFORMANCE_GUIDE.md` (§5ter-5quater), `docs/TESTING_STRATEGY.md` (§9ter), `docs/SECURITY_GUIDE.md` (§3quater) et `docs/REPOSITORY_PATTERN.md` (§2) — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 207 documents.
- **5 des 15 livrables demandés n'ont pas donné lieu à un fichier séparé** : `ZUSTAND_ARCHITECTURE.md` (recoupait `docs/TECHNOLOGY_COMPARISONS.md` §3 déjà existant — seules les comparaisons manquantes, Zustand vs MobX et vs Context API, ont été ajoutées en §3bis-3ter, Redux Toolkit était déjà couvert), `MEMOIZATION_GUIDE.md`/`STATE_PERFORMANCE.md` (recoupaient `docs/PERFORMANCE_GUIDE.md` §5-5bis déjà existants), `STATE_TESTING_GUIDE.md` (recoupait `docs/TESTING_STRATEGY.md`), `STATE_SECURITY.md` (recoupait `docs/SECURITY_GUIDE.md`) — chacun étendu plutôt que dupliqué.
- **Décision structurante prise et justifiée, pas simplement documentée** : `docs/STORE_SPECIFICATIONS.md` §3 audite les 17 noms de stores candidats du cadrage et conclut que seuls 13 sont des stores Zustand réels — `trackStore`, `statisticsStore`, `historyStore` et `favoriteStore` sont rejetés explicitement (état serveur ou dérivé déguisé en état client, violerait le principe « une donnée n'existe qu'une seule fois »), et un 13ème store non nommé par le cadrage (`uiStore`) est ajouté pour couvrir les préoccupations de présentation transverses (Sidebar/Fullscreen/Panels partagés, `docs/UI_STATE.md` §2). Décompte forcé à 17 aurait été un forçage esthétique plutôt qu'une décision architecturale honnête.
- **Gap réel trouvé et comblé pendant la rédaction, pas seulement anticipé** : `docs/SERVER_STATE.md` §5 a révélé que `docs/REPOSITORY_PATTERN.md` §2 (Phase 13) omettait `CollectionRepository` et `ArtworkRepository`, deux repositories réellement nécessaires pour couvrir les entités `Collection`/`Artwork` déjà définies dans `docs/DOMAIN_MODELS.md`. Ajoutés directement à `docs/REPOSITORY_PATTERN.md` plutôt que laissés comme une lacune documentée passivement.
- **Gap réel trouvé et comblé en sécurité** : `docs/SECURITY_GUIDE.md` §3quater comble une question jamais posée jusqu'ici — l'exposition de l'état applicatif via l'outillage de développement (devtools Zustand) en production. Décision : devtools désactivés hors développement, `developerStore`/`labsStore` jamais activés par défaut.
- `docs/EVENT_SYSTEM.md` §4 ferme explicitement la catégorie « Navigation Events » nommée par le cadrage — TanStack Router expose déjà ses propres hooks de changement de route, dupliquer ce mécanisme avec un événement applicatif aurait violé la non-duplication déjà actée.
- `docs/STATE_MANAGEMENT.md` §7 vérifie explicitement la cohérence avec les six documents nommés par le cadrage (`ARCHITECTURE.md`, `DATA_LAYER.md`, `REPOSITORY_PATTERN.md`, `DATABASE_SCHEMA.md`, `CACHE_SYSTEM.md`, `SYNC_ENGINE_SPECIFICATION.md`) — aucune contradiction trouvée, cette phase étend la couche donnée de la Phase 13 d'une couche d'état applicatif cohérente.
- Auto-revue effectuée : tous les `[[wikilinks]]` de `docs/` résolvent (même exception connue `[[Document.md]]`) ; deux wikilinks malformés introduits pendant la rédaction (syntaxe invalide type `[[Nom (parenthèse explicative)]]`) trouvés et corrigés avant la fin de la phase.
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : toutes les décisions produit/UX/visuelles/linguistiques/sensorielles/architecturales/de navigation/système des phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP) — cette phase achève la documentation de l'architecture frontend (données + état) ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### Moteur Audio (2026-08-04)

- 6 nouveaux documents rédigés dans `docs/` (cycle de vie du moteur, machine à états officielle, traduction commande→moteur, contrat de commandes, streaming Jellyfin, gestion du buffer) + extension de `docs/AUDIO_ENGINE.md` (promu capstone), `docs/PLAYBACK_DEVICES.md` (§7bis), `docs/EVENT_SYSTEM.md` (§3bis), `docs/PERFORMANCE_GUIDE.md` (§5quinquies), `docs/TESTING_STRATEGY.md` (§9quater), `docs/SECURITY_GUIDE.md` (§3quinquies) et `docs/REPOSITORY_PATTERN.md` (§2) — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 213 documents.
- **`docs/AUDIO_ENGINE.md` existait déjà depuis la Phase 0.5** — 9 des 15 livrables demandés (l'`AUDIO_ENGINE.md` lui-même, plus `QUEUE_ENGINE.md`, `MEDIA_SESSION.md`, `PLAYBACK_HISTORY.md`, `DEVICE_MANAGEMENT.md`, `PLAYBACK_EVENTS.md`, `AUDIO_PERFORMANCE.md`, `AUDIO_TESTING_GUIDE.md`, `AUDIO_SECURITY.md`) recoupaient des documents déjà profonds (Phase 0.5, Phase 9, Architecture d'état) — étendus plutôt que dupliqués, `AUDIO_ENGINE.md` promu capstone de cette phase.
- **Décision architecturale centrale, directement issue de la contrainte spécifique du projet** : `docs/AUDIO_ENGINE.md` §0bis.2 tranche que la priorité locale/cache/streaming est assurée exclusivement par une nouvelle méthode `TrackRepository.getPlaybackSource(trackId)` (ajoutée à `docs/REPOSITORY_PATTERN.md` §2) qui résout un `PlaybackSource` opaque (`{ uri, kind }`) — le Playback Controller (`docs/PLAYBACK_CONTROLLER.md`) est la seule couche qui consulte `kind` (pour choisir la stratégie de buffer), le Playback Engine et le Media Adapter ne reçoivent jamais que `uri`. Un changement de source en cours de lecture (téléchargement qui se termine pendant un streaming de la même piste) reste totalement transparent, cohérent avec l'exigence explicite du cadrage.
- Architecture en couches formalisée pour la première fois avec cette précision : UI → Player Store → Playback Controller → Playback Engine → Media Adapter → `HTMLAudioElement` → Audio Output (`docs/AUDIO_ENGINE.md` §0bis.1), une seconde application de Ports & Adapters à l'intérieur du domaine audio (le Playback Engine est le port, le Media Adapter l'implémentation concrète).
- `docs/PLAYBACK_STATE_MACHINE.md` introduit les 12 états officiels (Idle/Loading/Buffering/Ready/Playing/Paused/Seeking/Ended/Stopped/Error/Offline/Reconnecting) avec une distinction explicite `Stopped` vs `Paused` (intention de reprise) qui n'existait sous aucune forme dans la documentation précédente.
- `docs/AUDIO_ENGINE.md` §10 (Extensibilité) confirme honnêtement que l'architecture en couches accueille Crossfade/Gapless/Égaliseur/Visualiseur (déjà livrés)/DSP/Casting/Multiroom (déjà préparés) sans réécriture, mais signale explicitement Lecture collaborative/Lecture distante comme un gap non préparé — cohérent avec l'honnêteté déjà appliquée dans tout le projet plutôt que de prétendre à une extensibilité totale.
- Auto-revue effectuée : tous les `[[wikilinks]]` de `docs/` résolvent (même exception connue `[[Document.md]]`) ; deux wikilinks malformés introduits pendant la rédaction trouvés et corrigés avant la fin de la phase ; `docs/TABLE_OF_CONTENTS.md`, `docs/DOCUMENT_HIERARCHY.md` (ajout des niveaux 11bis/11ter) et `docs/DOCUMENT_DEPENDENCY_GRAPH.md` (chiffres recalculés, `AUDIO_ENGINE.md` entre dans le top 10 des documents les plus référencés avec 104 références entrantes) mis à jour conformément à la règle de gouvernance rappelée en tête de ce cadrage.
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : toutes les décisions produit/UX/visuelles/linguistiques/sensorielles/architecturales/de navigation/système des phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP) — cette phase achève la documentation de l'architecture frontend (données + état + moteur audio) ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### Moteur de Recherche (2026-08-04)

- 7 nouveaux documents rédigés dans `docs/` (constitution capstone, cycle de vie de l'index, spécification d'un index par entité, classement multi-facteurs, moteur de suggestions, moteur de filtres, moteur de tri) + extension de `docs/DATA_LAYER.md` (§3.5), `docs/RECOMMENDATION_ENGINE.md` (§5bis), `docs/PERFORMANCE_GUIDE.md` (§5sexies), `docs/TESTING_STRATEGY.md` (§9quinquies), `docs/EVENT_SYSTEM.md` (§3ter) et `docs/PERFORMANCE_BUDGET.md` (§2, amendement) — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 220 documents.
- **Contradiction réelle trouvée et résolue explicitement, pas silencieusement** : le cadrage exigeait un temps de réponse < 50 ms, plus strict que le budget perçu de bout en bout déjà engagé depuis la Phase 0.5 (< 100 ms, `docs/PERFORMANCE_BUDGET.md` §2). Résolu en distinguant deux budgets imbriqués — < 50 ms pour le calcul moteur seul (nouveau), < 100 ms pour le budget perçu incluant le rendu React (inchangé) — plutôt que d'écraser silencieusement l'un des deux chiffres déjà justifiés.
- **5 des 12 livrables demandés n'ont pas donné lieu à un fichier séparé** : `SEARCH_ALGORITHMS.md` (recoupait `docs/DATA_LAYER.md` §3.4 déjà écrit en Phase 13), `DISCOVERY_ENGINE.md` (recoupait fortement `docs/RECOMMENDATION_ENGINE.md` et `docs/DISCOVERY_SPECIFICATION.md` déjà profonds), `SEARCH_PERFORMANCE.md`/`SEARCH_TESTING_GUIDE.md`/`SEARCH_EVENTS.md` (recoupaient chacun un document déjà établi lors des phases précédentes) — chacun étendu plutôt que dupliqué.
- `docs/SEARCH_ENGINE.md` devient le capstone du domaine — n'existait sous aucune forme séparée auparavant (seule une sous-section de `docs/DATA_LAYER.md` §3 couvrait le sujet), créé en tant que nouveau document plutôt qu'extension parce que le périmètre demandé (index engine, ranking, suggestion, filter, sort, discovery) dépassait largement ce qu'une sous-section pouvait porter sans déséquilibrer `docs/DATA_LAYER.md`.
- `docs/RANKING_ENGINE.md` introduit un classement à deux niveaux explicitement hiérarchisés (pertinence textuelle toujours dominante, score composite comme multiplicateur borné jamais additif sans plafond) — empêche structurellement qu'un facteur de popularité fasse remonter un résultat peu pertinent au-dessus d'un résultat très pertinent.
- `docs/SEARCH_INDEX_SPECIFICATION.md` §2 traite honnêtement Compositeurs et Paroles comme préparés, non engagés : aucun champ `composer` structuré n'existe encore dans `docs/DOMAIN_MODELS.md`, et l'indexation des paroles dépend d'une disponibilité déjà signalée comme non résolue (`docs/SEARCH_SPECIFICATION.md` §3, `docs/PLAYER_SPECIFICATION.md` §6).
- `docs/SEARCH_ENGINE.md` §0bis.4 prépare l'architecture multi-serveurs (un index par serveur, fusion à la présentation) sans l'implémenter — cohérent avec YAGNI, la frontière est déjà la bonne, sa mise en œuvre attend un besoin réel engagé (un seul serveur actif par défaut à ce jour).
- Auto-revue effectuée : tous les `[[wikilinks]]` de `docs/` résolvent (même exception connue `[[Document.md]]`) ; `docs/TABLE_OF_CONTENTS.md`, `docs/DOCUMENT_HIERARCHY.md` (ajout du niveau 11quater, pair du Moteur Audio) et `docs/DOCUMENT_DEPENDENCY_GRAPH.md` (chiffres recalculés — `PERFORMANCE_BUDGET.md` et `DATA_LAYER.md` entrent dans le top 10 des documents les plus référencés) mis à jour.
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : toutes les décisions produit/UX/visuelles/linguistiques/sensorielles/architecturales/de navigation/système des phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP) — cette phase achève la documentation de l'architecture frontend (données + état + moteur audio + moteur de recherche) ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### Plateforme Offline (2026-08-04)

- 4 nouveaux documents rédigés dans `docs/` (résolution de conflit entité par entité, politique de stockage globale, guide de résilience et recovery, registre des tâches de fond) + extension de `docs/OFFLINE_SYSTEM.md` (promu capstone), `docs/SYNC_ENGINE_SPECIFICATION.md` (§2bis, §4bis), `docs/CACHE_SYSTEM.md` (§8bis), `docs/DOWNLOAD_SYSTEM.md` (§5quinquies), `docs/PERFORMANCE_GUIDE.md` (§5septies), `docs/SECURITY_GUIDE.md` (§3sexies), `docs/DIAGNOSTICS_SYSTEM.md` (§6bis) et `docs/TESTING_STRATEGY.md` (§9sexies) — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 224 documents.
- **Phase la plus consolidation-lourde depuis la Phase 12** : sur les 15 livrables demandés, seuls 4 étaient réellement nouveaux — `docs/CACHE_SYSTEM.md` en particulier recoupait presque intégralement `CACHE_LIFECYCLE.md` demandé (création/mise à jour/préchargement/nettoyage/expiration/réparation/compression déjà tous couverts section par section depuis la Phase 11), réduit à une simple table récapitulative plutôt qu'une redite.
- **`docs/OFFLINE_SYSTEM.md` promu capstone** malgré un nom différent du livrable demandé (`OFFLINE_PLATFORM.md`) — même pattern déjà appliqué à `docs/AUDIO_ENGINE.md` et `docs/DATA_LAYER.md` : le domaine prime sur la correspondance exacte du nom.
- **Gap réel comblé, jamais nommé explicitement jusqu'ici** : `docs/SYNC_ENGINE_SPECIFICATION.md` §4bis introduit le journal de changements locaux (favoris/playlists modifiés hors ligne mis en file pour remontée serveur) — présupposé depuis la Phase 9 (`docs/OFFLINE_SYSTEM.md` §4, « changements effectués hors ligne... vers l'état serveur ») mais jamais mécanisé jusqu'à cette phase.
- **Décision honnête plutôt que forcée** : `docs/CONFLICT_RESOLUTION.md` §3 établit qu'Historique et Statistiques n'ont **jamais** de conflit par construction (journaux append-only) — plutôt que d'inventer un mécanisme de résolution pour un cas qui ne peut structurellement pas se produire.
- `docs/RESILIENCE_GUIDE.md` fusionne délibérément les deux livrables demandés `RESILIENCE_GUIDE.md` et `RECOVERY_ENGINE.md` en un seul document — la taxonomie de défaillance et le mécanisme de recovery n'ont de sens que l'un accompagnant l'autre, généralisant le pattern à 3 paliers déjà acté pour le moteur audio (`docs/PLAYBACK_ENGINE.md` §5) à toute la plateforme.
- `docs/OFFLINE_SYSTEM.md` §0bis.4 (Extensibilité) traite honnêtement la synchronisation cloud comme une tension déjà signalée avec la charte (`docs/EVOLVABILITY.md` §12) plutôt qu'une simple préparation — et la lecture collaborative comme un gap non préparé, cohérent avec le même traitement déjà acté pour le moteur audio.
- Auto-revue effectuée : tous les `[[wikilinks]]` de `docs/` résolvent (même exception connue `[[Document.md]]`) ; `docs/TABLE_OF_CONTENTS.md`, `docs/DOCUMENT_HIERARCHY.md` (ajout du niveau transverse Plateforme Offline, seule exception documentée au principe qu'une flèche ne remonte jamais dans la hiérarchie) et `docs/DOCUMENT_DEPENDENCY_GRAPH.md` (chiffres recalculés) mis à jour.
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : toutes les décisions produit/UX/visuelles/linguistiques/sensorielles/architecturales/de navigation/système des phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP) — cette phase achève la documentation de l'architecture frontend (données + état + moteur audio + moteur de recherche + plateforme offline) ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### Engineering Handbook (2026-08-04)

- 5 nouveaux documents rédigés dans `docs/` (capstone, guide TypeScript, checklist du relecteur, definition of ready, tableau de bord d'ingénierie) + extension de `docs/FRONTEND_ARCHITECTURE.md` (§8bis-8ter), `docs/GIT_WORKFLOW.md` (§2, §3.0bis, §4bis-4ter), `docs/TESTING_STRATEGY.md` (§9septies-9octies), `docs/FEATURE_FLAGS.md` (§6bis), `docs/CHECKLISTS.md` (§4) et `docs/DEVELOPMENT_GUIDELINES.md` (étape 9, Monitoring) — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 229 documents.
- **Record de consolidation du projet** : sur les 19 livrables demandés, seuls 5 étaient réellement nouveaux (26 %) — 6 existaient déjà sous le nom exact demandé (`CODING_STANDARDS.md`, `GIT_WORKFLOW.md`, `QUALITY_GATES.md`, `CI_CD_GUIDE.md`, `PERFORMANCE_BUDGET.md`, `DEFINITION_OF_DONE.md`), conséquence directe de la profondeur déjà atteinte en Phase 0/0.5. Aucune tentative de forcer un nouveau fichier pour respecter la lettre du cadrage au détriment de la cohérence déjà actée.
- **Aucune modification nécessaire** : `docs/ACCESSIBILITY_GUIDE.md` a été vérifié contre la demande explicite « WCAG AA, objectif AAA » (`ACCESSIBILITY_CHECKLIST.md` demandé) — déjà acté mot pour mot depuis `docs/PROJECT_CHARTER.md` §3.6 et déjà rappelé dans le cadrage du document lui-même. Documenté comme un cas de « zéro changement » plutôt que de forcer une extension cosmétique.
- **`docs/CODE_REVIEW_GUIDE.md` distingué explicitement de `docs/DEFINITION_OF_DONE.md`** : le premier est le point de vue du relecteur (que vérifier, dans quel ordre), le second celui de l'auteur (checklist à cocher avant de demander une revue) — les deux se recoupent par nature (même standard final) sans jamais être interchangeables, cohérent avec la distinction déjà établie entre `docs/QUALITY_GATES.md` (automatisé) et `docs/DEFINITION_OF_DONE.md` (humain + automatisé).
- **`docs/DEFINITION_OF_READY.md` comme miroir honnête** : plutôt que d'inventer des critères non actés, le document renvoie explicitement à `docs/DEVELOPMENT_GUIDELINES.md` §1 (déjà acté depuis la Phase 0) pour la substance, et se limite à formaliser la checklist elle-même — cohérent avec la discipline de non-duplication appliquée à travers tout le projet.
- **Statut honnête plutôt qu'un chiffre inventé** : `docs/ENGINEERING_METRICS.md` §4 signale explicitement qu'aucun budget de temps de build CI n'est engagé, faute de mesure réelle possible (aucun code applicatif n'existe encore) — un chiffre cible aurait été une valeur inventée, jamais mesurée, contraire à l'honnêteté déjà appliquée dans tout le corpus.
- Auto-revue effectuée : tous les `[[wikilinks]]` de `docs/` résolvent (même exception connue `[[Document.md]]`) ; `docs/TABLE_OF_CONTENTS.md`, `docs/DOCUMENT_HIERARCHY.md` (les 5 nouveaux documents rejoignent le niveau 0 Gouvernance, même nature que les 13 déjà présents) et `docs/DOCUMENT_DEPENDENCY_GRAPH.md` (chiffres recalculés, `docs/CODING_STANDARDS.md` entre dans le top 10) mis à jour.
- Aucun code applicatif écrit — voulu, cette phase reste documentaire.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : toutes les décisions produit/UX/visuelles/linguistiques/sensorielles/architecturales/de navigation/système des phases précédentes, entrée effective en Phase 1 d'ingénierie (MVP) — cette phase achève la documentation de gouvernance d'ingénierie ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### Implementation Plan (2026-08-05)

- 13 nouveaux documents rédigés dans `docs/` (capstone, 15 Epics, 82 Features, 45 Tasks décomposées pour M0-M3, graphe de dépendances/chemin critique, jalons M0-M14, portée MVP, plan de release, guide GitHub Projects, labels, checklists d'implémentation, risques de séquencement, backlog maître) — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 242 documents.
- **Première phase sans aucune extension de document existant** : les 13 livrables sont tous réellement nouveaux — le domaine (planification de projet exécutable) n'existait sous aucune forme dans le corpus, contrairement à toutes les phases précédentes qui recoupaient systématiquement du contenu déjà écrit.
- **Décision méthodologique annoncée avant exécution, pas découverte après coup** : `docs/TASK_BREAKDOWN.md` §1 assume explicitement le rolling wave planning (élaboration progressive) — décomposition complète jusqu'au niveau Subtask pour les 4 jalons fondateurs (M0-M3, 45 Tasks), Features seulement pour M4-M14. Justifié explicitement : une décomposition Subtask de l'intégralité des 229 documents d'architecture aurait produit plusieurs milliers de tâches spéculatives condamnées à être invalidées par des détails d'implémentation encore inconnus — violation directe de YAGNI, déjà cité dans `docs/ARCHITECTURE_PRINCIPLES.md` §8bis.
- **Granularité Epic = Jalon, décision structurante assumée** : `docs/EPICS.md` §1 fait le choix explicite d'un Epic par jalon (15 Epics pour 15 jalons M0-M14) plutôt qu'une hiérarchie Epic indépendante du calendrier — évite de maintenir deux structures parallèles qui pourraient diverger.
- **Règle du cadrage appliquée concrètement, pas seulement énoncée** : `docs/TASK_BREAKDOWN.md` §9 illustre la redécoupe obligatoire d'une Task estimée L (Dialog, TASK-024) en deux Tasks M avant planification — exemple réel dans le backlog plutôt qu'une règle abstraite non démontrée.
- **Chemin critique identifié avec justification, pas par défaut** : `docs/DEPENDENCY_GRAPH.md` §3 explique précisément pourquoi EPIC-007 (Lecteur Audio) est sur le chemin critique alors qu'EPIC-006 (Bibliothèque) et EPIC-008 (Recherche) ne le sont pas — EPIC-009 (Téléchargements) dépend structurellement de la résolution de source du lecteur (`docs/AUDIO_ENGINE.md` §0bis.2), pas des deux autres.
- `docs/TECHNICAL_RISKS.md` distingué explicitement de `docs/RISK_REGISTER_TECHNICAL.md` (déjà existant, Phase 0.5) : le premier couvre le risque de *séquencement* du backlog, le second le risque *par décision d'architecture* — jamais de duplication du contenu déjà présent dans le second.
- Auto-revue effectuée conformément aux 5 vérifications explicitement demandées (`docs/IMPLEMENTATION_ROADMAP.md` §5) : toutes les fonctionnalités documentées apparaissent dans le backlog, aucune tâche orpheline, dépendances cohérentes, MVP réalisable, ordre des jalons logique. Tous les `[[wikilinks]]` de `docs/` résolvent (même exception connue `[[Document.md]]`) ; `docs/TABLE_OF_CONTENTS.md`, `docs/DOCUMENT_HIERARCHY.md` (nouveau niveau « au-dessus de la hiérarchie », jamais consommé en retour par l'architecture) et `docs/DOCUMENT_DEPENDENCY_GRAPH.md` (chiffres recalculés, `docs/AUDIO_ENGINE.md` dépasse `docs/DATA_LAYER.md` dans le top 10) mis à jour.
- Aucun code applicatif écrit — voulu, cette phase reste un plan, pas une implémentation.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : le projet dispose désormais d'une architecture complète (229 documents) et d'un plan d'exécution (13 documents, 242 au total) — la prochaine étape est le premier commit de code réel (TASK-001, `docs/ENGINEERING_BACKLOG.md` §3), jamais engagé sans confirmation explicite de l'utilisateur ; toutes les décisions produit/UX/visuelles/linguistiques/sensorielles encore ouvertes des phases précédentes le restent, ce plan ne les tranche pas ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

> Un second cadrage « Implementation Plan » quasi identique (~85-90 % de recoupement) a été soumis juste après celui-ci — signalé explicitement à l'utilisateur via une table de comparaison plutôt que refait ou ignoré silencieusement, confirmé comme doublon accidentel (« ne rien refaire »). Seul écart réel identifié, `SPRINT_PLAN.md`, non créé faute de demande explicite de l'utilisateur — aucune action prise, pas une entrée de phase distincte.

### Developer Playbook (2026-08-05)

- 1 nouveau document rédigé dans `docs/` (`docs/DEVELOPER_PLAYBOOK.md`), aucune extension — voir `docs/DOCUMENTATION_GUIDE.md` §1 pour la carte complète des 243 documents.
- Demande explicitement bornée par l'utilisateur : un document unique, concis (10-20 pages max), couvrant huit sujets précis (sélection de tâche, lecture des documents de référence, ADR, implémentation, tests, documentation, auto-review, definition of done), suivi d'une instruction explicite de ne rien développer ensuite.
- **Aucun contenu redécidé** : chacune des huit sections renvoie au document qui fait déjà autorité (`docs/DEVELOPMENT_GUIDELINES.md`, `docs/ENGINEERING_BACKLOG.md`, `docs/TASK_BREAKDOWN.md`, `docs/ADR_TEMPLATE.md`, `docs/TESTING_STRATEGY.md`, `docs/CODE_REVIEW_GUIDE.md`, `docs/DEFINITION_OF_DONE.md`, `docs/CODING_STANDARDS.md`) — la valeur ajoutée du document est l'ordre de lecture et le parcours narratif unique, jamais une règle nouvelle.
- `docs/DEVELOPER_PLAYBOOK.md` rejoint le niveau 0 Gouvernance ([[DOCUMENT_HIERARCHY.md]] §2), même nature que les documents Engineering Handbook déjà présents.
- Auto-revue effectuée : tous les `[[wikilinks]]` de `docs/` résolvent (même exception connue `[[Document.md]]`) ; `docs/TABLE_OF_CONTENTS.md`, `docs/DOCUMENT_HIERARCHY.md` et `docs/DOCUMENT_DEPENDENCY_GRAPH.md` mis à jour avec un recomptage exact (242→243 documents, 6 582→6 645 wikilinks) — `docs/DEVELOPER_PLAYBOOK.md` signalé honnêtement comme le seul document du corpus sans référence entrante à ce stade, plutôt que masqué.
- Aucun code applicatif écrit — voulu, conformément à l'instruction explicite de l'utilisateur de ne rien développer après ce document.
- Non poussé sur `origin/main` à ce stade.
- Ouvert : identique à l'entrée précédente — la prochaine étape reste TASK-001, jamais engagée sans confirmation explicite ; toutes les décisions produit/UX/visuelles/linguistiques/sensorielles encore ouvertes le restent ; reprise de l'extraction du graphe de connaissances (2 chunks sur 3 toujours en attente).

### TASK-001 (2026-08-05)

> **Premier code applicatif du projet.** Toutes les entrées précédentes de ce journal affirmaient « aucun code applicatif écrit à ce jour » — c'était vrai à leur date. Cette entrée marque le point où cela cesse d'être vrai.

- Confirmation explicite de l'utilisateur : « Nous passons officiellement en phase d'implémentation. » — implémentation de **TASK-001 uniquement** (`docs/TASK_BREAKDOWN.md` §5, EPIC-001/M0, STORY-001), aucune autre Task engagée.
- Documents de référence lus avant implémentation : `docs/ARCHITECTURE_REVIEW.md` (méthode d'audit déjà pratiquée), `docs/DOCUMENTATION_CHECKLIST.md` §4 (checklist exacte à automatiser), `docs/EPICS.md`/`docs/MILESTONES.md`/`docs/ENGINEERING_BACKLOG.md` (confirmation que TASK-001 est bien la Task sans dépendance, prête). Aucun ADR nécessaire — Task de vérification pure, aucune décision structurante, aucune nouvelle dépendance (cohérent avec `docs/DEVELOPER_PLAYBOOK.md` §4, test rapide).
- **Résultat** : `scripts/verify-docs.mjs` + `scripts/lib/doc-graph.mjs` — vérification automatisée des wikilinks/citations sur `docs/`, zéro dépendance npm (M0 précède l'initialisation du workspace pnpm, TASK-006+ ; installer un test runner/lint maintenant aurait anticipé le périmètre d'une autre Task, contraire à l'instruction explicite de l'utilisateur de ne modifier aucun autre composant). Exécuté sur les 243 documents actuels (la Task cite « 229 », chiffre du corpus au moment de sa rédaction avant les phases Implementation Plan/Developer Playbook — écart signalé explicitement plutôt que corrigé en silence, le script vérifie l'état réel du dépôt).
- **Résultats de la vérification** : 0 lien cassé sur 6 658 wikilinks ; 0 document sans référence entrante ; 12 citations de section signalées comme potentiellement non résolues par l'heuristique (ex. `SECURITY_GUIDE.md`/`STACK_DECISIONS.md`/`STATISTICS_ENGINE.md`/`TESTING_STRATEGY.md` citent `PERFORMANCE_BUDGET.md` §0, qui n'existe pas ; `ARTWORK_SYSTEM.md`/`CACHE_SYSTEM.md`/`CARD_SPECIFICATION.md` citent `PREMIUM_DETAILS.md` §33 ; `AUDIO_VISUALIZER.md`/`PREMIUM_EXPERIENCE_BIBLE.md` citent `MOTION_GUIDELINES.md` §13.4 ; `ARTIST_SCREEN.md` cite `SCREEN_SYSTEM.md` §1.5 ; `ENGINEERING_HANDBOOK.md` cite `CODE_REVIEW_GUIDE.md` §2.4 ; `PROJECT_CHARTER.md` cite `ROADMAP.md` §5). **Non corrigées dans cette Task** — leur revue et correction appartiennent explicitement à TASK-002/TASK-003, jamais improvisées ici.
- **Tests** : 11 tests unitaires (`node:test`, zéro dépendance) sur la logique pure du graphe de documents (`scripts/lib/doc-graph.test.mjs`) — extraction de wikilinks, extraction de citations de section, détection de liens cassés, détection d'orphelins, résolution de citations. Une régression trouvée et corrigée pendant l'écriture des tests eux-mêmes : le regex de section ne supportait qu'un suffixe (`bis`/`ter`...) *avant* un nombre décimal, jamais après (`§3.0bis` tronqué en `§3.0`) — corrigé, testé (`extractHeadingNumbers and extractSectionCitations agree on a dotted number followed by a suffix`), confirmé par la disparition du faux positif correspondant sur `docs/DEVELOPER_PLAYBOOK.md` entre les deux exécutions.
- **Documentation mise à jour** : `docs/ENGINEERING_BACKLOG.md` §1/§3 (statut M0, prochaine action → TASK-002) — aucun document fondateur d'architecture modifié, aucun standard n'a changé.
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (script autonome hors monorepo, ne préempte aucune Task future) ; lisibilité (fonctions pures nommées, commentaires uniquement sur les contraintes non évidentes) ; performance/accessibilité/sécurité non applicables (outil CLI local, aucune donnée sensible, aucune entrée externe) ; tests exécutés localement (11/11) ; documentation relue.
- Non commité, non poussé sur `origin/main` à ce stade — diff proposé à l'utilisateur pour revue avant tout commit.
- Ouvert : TASK-002 (revue croisée manuelle des 10 documents à plus forte cascade, doit statuer sur les 12 citations signalées) est la prochaine Task exploitable ; toutes les décisions produit/UX/visuelles/linguistiques/sensorielles encore ouvertes des phases précédentes le restent.

### TASK-002 (2026-08-05)

- Confirmation explicite de l'utilisateur : « Nous continuons le développement en suivant strictement le backlog officiel [...] Passe maintenant à TASK-002. » — implémentation de **TASK-002 uniquement** (`docs/TASK_BREAKDOWN.md` §5, EPIC-001/M0, STORY-001, dépend de TASK-001).
- Documents de référence identifiés et lus intégralement avant toute modification : les 10 documents à plus forte cascade (`docs/DOCUMENT_DEPENDENCY_GRAPH.md` §1) — `ARCHITECTURE_PRINCIPLES.md`, `PROJECT_CHARTER.md`, `PERFORMANCE_BUDGET.md`, `PRODUCT_RULES.md`, `AUDIO_ENGINE.md`, `DATA_LAYER.md`, `MOTION_GUIDELINES.md`, `CODING_STANDARDS.md`, `PLAYER_SPECIFICATION.md`, `ENGINEERING_GUIDE.md`. Dépendance vérifiée (TASK-001 terminée). Aucun ADR nécessaire — revue et correction de citations, aucune décision structurante.
- **Deux catégories de défauts réels trouvés et corrigés**, strictement limitées aux documents en périmètre :
  1. **Citations vers une section inexistante** (héritées des 12 signalées par TASK-001) — seules celles ciblant un document du top 10 relevaient de TASK-002 : `PERFORMANCE_BUDGET.md` §0 (n'existe pas, 4 documents citants : `SECURITY_GUIDE.md`, `STACK_DECISIONS.md`, `STATISTICS_ENGINE.md`, `TESTING_STRATEGY.md`) corrigé en §8 (Méthodologie de vérification — section qui définit réellement la bibliothèque de test de 200 000+ titres que ces 4 citations invoquent) ; `MOTION_GUIDELINES.md` §13.4 (n'existe pas comme en-tête — §13 est une liste à 5 règles non sous-numérotées, `AUDIO_VISUALIZER.md`/`PREMIUM_EXPERIENCE_BIBLE.md`, plus une auto-citation interne au même document) corrigé en §13. Les 6 citations restantes (`SCREEN_SYSTEM.md`, `PREMIUM_DETAILS.md` ×3, `CODE_REVIEW_GUIDE.md`, `ROADMAP.md`) ne ciblent aucun document du top 10 — **non corrigées**, explicitement laissées pour une Task future.
  2. **Défaut systémique nouvellement trouvé, pas dans la liste de TASK-001** : 8 des 10 documents du top 10 ont un numéro de version affiché en en-tête désynchronisé de leur propre tableau « Historique des révisions » (jamais mis à jour lors d'amendements successifs) — ex. `AUDIO_ENGINE.md` affichait « 0.1.0 » alors que son propre tableau allait jusqu'à « 1.0.0 ». Corrigé sur les 8 documents concernés (`ARCHITECTURE_PRINCIPLES.md`, `PERFORMANCE_BUDGET.md`, `PRODUCT_RULES.md`, `AUDIO_ENGINE.md`, `DATA_LAYER.md`, `CODING_STANDARDS.md`, `PLAYER_SPECIFICATION.md`, `MOTION_GUIDELINES.md`). **Mesure d'ampleur réelle effectuée** (diagnostic ponctuel, non committé comme outil) : 63 documents sur 243 restent affectés corpus-entier — hors périmètre strict de TASK-002 (seuls les 10 documents à plus forte cascade en relevaient), signalé explicitement plutôt que silencieusement corrigé partout ou ignoré, candidat pour une Task dédiée future ou pour le rapport de TASK-003.
- **Auto-piège trouvé et corrigé en cours de travail** : les premières entrées de journal de révision rédigées pour documenter les corrections citaient le texte fautif entre `[[doubles crochets]]` (ex. « `[[PERFORMANCE_BUDGET.md]] §0` »), ce qui recréait de nouvelles occurrences du même défaut que `scripts/verify-docs.mjs` venait de signaler — le script les a immédiatement re-détectées à la vérification suivante (12 au lieu de 6 attendus). Corrigé en reformulant ces 6 entrées sans crochets doubles (texte descriptif plutôt que wikilink) — aucune régression du script lui-même, un artefact d'auto-documentation, pas un bug de code.
- **Tests** : aucune modification de `scripts/lib/doc-graph.mjs` cette Task (uniquement des corrections de contenu documentaire) — suite existante (11 tests) réexécutée à trois reprises pendant la Task comme garde-fou de non-régression, toujours au vert. Pas de nouveau test ajouté : le comportement du script face à un wikilink cité dans un changelog est correct (il ne peut pas distinguer une citation réelle d'une citation entre guillemets dans une phrase), la leçon est une discipline d'écriture (ne jamais entourer de `[[...]]` un texte cité qui n'est pas une vraie référence), pas un défaut à figer par un test.
- **Documentation mise à jour** : `docs/ENGINEERING_BACKLOG.md` §1/§3 (statut M0, prochaine action → TASK-003) ; version + tableau de révisions des 10 documents corrigés + des 6 documents citants édités (16 fichiers au total avec un nouveau numéro de version).
- Vérification finale : `node scripts/verify-docs.mjs` → 0 lien cassé (6 659 wikilinks), 0 document orphelin, 6 citations suspectes restantes (toutes hors périmètre top 10, confirmé) ; `node --test scripts/lib/doc-graph.test.mjs` → 11/11.
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (aucune, changements de contenu documentaire uniquement) ; lisibilité (citations corrigées vers la section réellement pertinente, jamais un numéro arbitraire) ; performance/accessibilité/TypeScript non applicables (aucun code applicatif modifié) ; sécurité non applicable ; tests exécutés (11/11, aucune régression) ; documentation relue par re-vérification automatisée après chaque lot de corrections.
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : TASK-003 (consolidation du rapport final pré-implémentation) est la prochaine Task exploitable — devra statuer sur les 6 citations non résolues restantes et sur le défaut systémique de version d'en-tête (63 documents) trouvé mais volontairement non traité hors périmètre ; toutes les décisions produit/UX/visuelles/linguistiques/sensorielles encore ouvertes des phases précédentes le restent.

### TASK-003 (2026-08-05)

- Confirmation explicite de l'utilisateur : « Termine M0 sans modifier le backlog. Commence par implémenter uniquement TASK-003. » — implémentation de **TASK-003 uniquement** (`docs/TASK_BREAKDOWN.md` §5, EPIC-001/M0, STORY-001, dépend de TASK-002), avec quatre contraintes explicites : périmètre strict, ne pas anticiper TASK-004/TASK-005, aucune nouvelle Task créée, aucune décision d'architecture modifiée.
- Avant implémentation, l'utilisateur avait demandé un état des lieux de TASK-003 (titre, objectif, position, priorité, tâches débloquées, documents utilisés) sans exécution — répondu en signalant explicitement que ce projet n'a pas de « Sprint » au sens du backlog retenu (concept issu du cadrage dupliqué refusé) et en mappant la question sur le jalon M0 réellement défini (`docs/MILESTONES.md` §1) ; confirmé que TASK-003/004/005 restent une série purement documentaire (`docs/EPICS.md`, EPIC-001 : « Aucun développement »).
- Documents de référence : `docs/ARCHITECTURE_REVIEW.md` (gabarit de rapport ponctuel déjà pratiqué), `docs/DOCUMENTATION_CHECKLIST.md`, `docs/MILESTONES.md` §1 (critère de sortie M0 à satisfaire, pas à déclarer). Aucun ADR nécessaire — consolidation pure, aucune décision structurante.
- **Résultat** : `docs/PRE_IMPLEMENTATION_REPORT.md` — rapport ponctuel (même statut que `docs/ARCHITECTURE_REVIEW.md`, « pas un document vivant ») consolidant les résultats déjà produits par TASK-001 et TASK-002 : 244 documents, 0 lien cassé, 6 citations de section non résolues restantes (listées nommément), 54 documents avec version d'en-tête désynchronisée restants (sur 63 trouvés, 9 déjà corrigés par TASK-002). Le rapport énonce explicitement ce qu'il ne fait pas — pas de vérification de `docs/DOCUMENTATION_CHECKLIST.md` §1 sur le backlog (TASK-004), pas de déclaration de sortie de M0 (TASK-005) — pour respecter la contrainte de ne pas anticiper.
- **Aucune correction supplémentaire apportée** aux éléments déjà signalés ouverts (6 citations, 54 désynchronisations) — strictement de la consolidation, cohérent avec la contrainte « ne pas modifier de décision d'architecture » et le périmètre XS de la Task.
- **Tests** : aucune modification de `scripts/` cette Task (rapport markdown uniquement) — suite existante (11 tests) réexécutée comme garde-fou, toujours au vert. `scripts/verify-docs.mjs` exécuté deux fois (avant et après création du rapport) pour garantir que les chiffres consolidés reflètent l'état réel du dépôt au moment de l'écriture, pas une estimation.
- **Documentation mise à jour** : `docs/ENGINEERING_BACKLOG.md` §1/§3 (statut M0, prochaine action → TASK-004, explicitement non commencée) ; `docs/TABLE_OF_CONTENTS.md` (§22 Implementation Plan, 13→14 documents, total 243→244) ; `docs/DOCUMENT_DEPENDENCY_GRAPH.md` (chiffres recalculés, §3 — `PRE_IMPLEMENTATION_REPORT.md` devient le nouveau seul document sans référence entrante, `DEVELOPER_PLAYBOOK.md` n'est plus orphelin) ; `docs/DOCUMENTATION_GUIDE.md` §1 (carte du nouveau document).
- Vérification finale : `node scripts/verify-docs.mjs` → 244 documents, 6 668 wikilinks, 0 lien cassé, 1 document sans référence entrante (`PRE_IMPLEMENTATION_REPORT.md`, attendu pour un document neuf) ; `node --test scripts/lib/doc-graph.test.mjs` → 11/11.
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (aucune, rapport markdown pur, aucune décision d'architecture touchée — contrainte explicite respectée) ; performance/sécurité/accessibilité/TypeScript non applicables (aucun code) ; tests exécutés (11/11, aucune régression) ; documentation relue et vérifiée par re-exécution automatisée.
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : TASK-004 (vérifier l'application de `docs/DOCUMENTATION_CHECKLIST.md` §1 à ce backlog) est la prochaine Task exploitable — non commencée, conformément à la contrainte explicite de ne pas l'anticiper ; les 6 citations et 54 désynchronisations de version listées dans `docs/PRE_IMPLEMENTATION_REPORT.md` restent ouvertes ; toutes les décisions produit/UX/visuelles/linguistiques/sensorielles encore ouvertes des phases précédentes le restent.

### TASK-004 (2026-08-05)

- Confirmation explicite de l'utilisateur : « Termine M0 sans modifier le backlog. Commence par implémenter uniquement TASK-004. » Contraintes explicites : périmètre strict, ne pas anticiper TASK-005, ne pas créer de nouvelle Task, ne pas modifier de décision d'architecture, ne pas créer de nouveau document sauf exigence explicite de la Task.
- **Écart de cadrage signalé avant exécution, pas résolu en silence** : le message de l'utilisateur décrivait TASK-004 comme la vérification de « l'ensemble du backlog et de la documentation » contre « les règles » (plurielles) de `docs/DOCUMENTATION_CHECKLIST.md` — la spécification réelle (`docs/TASK_BREAKDOWN.md` §5) limite littéralement la Task à **§1 uniquement**, appliqué à **« ce backlog »** (les 14 documents du domaine Implementation Plan, `docs/TABLE_OF_CONTENTS.md` §22), pas à l'intégralité des 244 documents. Résolu en auditant l'intégralité de `docs/DOCUMENTATION_CHECKLIST.md` comme méthode (conformément à l'instruction explicite « vérification systématique de chaque point »), mais en limitant toute correction au périmètre réel de la Task (§1, backlog) — cohérent avec l'instruction de l'utilisateur elle-même de ne corriger que « les non-conformités entrant dans le périmètre de TASK-004 » et de ne « jamais effectuer de corrections opportunistes ».
- Documents de référence lus intégralement avant toute modification : les 14 documents du backlog (`docs/IMPLEMENTATION_ROADMAP.md`, `docs/EPICS.md`, `docs/FEATURES.md`, `docs/TASK_BREAKDOWN.md`, `docs/DEPENDENCY_GRAPH.md`, `docs/MILESTONES.md`, `docs/MVP_ROADMAP.md`, `docs/RELEASE_PLAN.md`, `docs/PROJECT_BOARD_GUIDE.md`, `docs/GITHUB_LABELS.md`, `docs/IMPLEMENTATION_CHECKLISTS.md`, `docs/TECHNICAL_RISKS.md`, `docs/ENGINEERING_BACKLOG.md`, `docs/PRE_IMPLEMENTATION_REPORT.md`) + `docs/DOCUMENTATION_CHECKLIST.md` en entier.
- TASK-003 vérifiée terminée avant de commencer : rapport produit, consolide bien TASK-001/002 sans anticiper TASK-004/005 (vérifié par relecture de sa propre section « Ce que ce rapport ne fait pas »), `docs/ENGINEERING_BACKLOG.md` à jour. Aucun ADR nécessaire — Task de validation pure, aucune décision structurante.
- **Vérification §1 sur les 14 documents du backlog — 0 non-conformité réelle trouvée.** Quatre risques de collision de nom explicitement vérifiés (comme la discipline déjà appliquée depuis la Phase 6) : `DEPENDENCY_GRAPH.md` vs `COMPONENT_DEPENDENCY_GRAPH.md`/`STORE_DEPENDENCY_GRAPH.md`/`DOCUMENT_DEPENDENCY_GRAPH.md` (quatre domaines réellement distincts, confirmé) ; `TECHNICAL_RISKS.md` vs `RISK_REGISTER_TECHNICAL.md` (risques de séquencement vs risques par décision d'architecture, cadrage explicite déjà présent en tête de document) ; `MVP_ROADMAP.md` vs `FEATURE_ROADMAP.md` (traduction en Epics/Features vs priorisation fonctionnelle, cadrage explicite présent) ; `IMPLEMENTATION_CHECKLISTS.md` vs `DEFINITION_OF_DONE.md`/`CODE_REVIEW_GUIDE.md` (gabarit compact vs critères complets, cadrage explicite présent). Aucune fusion nécessaire — cohérent avec le constat déjà établi par `docs/ARCHITECTURE_REVIEW.md` §2 (très peu de doublons réels dans un corpus déjà discipliné).
- Vérification méthode (hors périmètre de correction) des autres sections de `docs/DOCUMENTATION_CHECKLIST.md` sur les mêmes 14 documents : §2 (en-tête complet, cadrage, checklist de validation, historique des révisions) conforme sur les 14 ; §3 (modification d'un document à forte cascade) déjà respecté lors de TASK-002 ; §4 (audit de cohérence) reconfirmé par réexécution du script ; §5 (densité) conforme, aucun document sous 50 lignes ni disproportionné sans justification. Aucune non-conformité trouvée dans ces sections non plus — aucune correction nécessaire, donc aucune n'a été appliquée.
- **Limite assumée** : la cohérence terminologique avec `docs/GLOSSARY.md` (§2) a été vérifiée par échantillonnage, pas terme par terme sur les 14 documents — au-delà du périmètre raisonnable d'une Task XS.
- **Aucun fichier de contenu du backlog modifié** — zéro non-conformité trouvée dans le périmètre réel de la Task signifie zéro correction à apporter, cohérent avec l'instruction explicite de ne jamais effectuer de correction opportuniste. Aucun nouveau document créé — TASK-004 ne l'exige pas explicitement.
- **Scripts exécutés** : `node scripts/verify-docs.mjs` (avant et après, aucun changement de contenu donc résultat identique : 244 documents, 6 678 wikilinks, 0 lien cassé, 0 orphelin, 6 citations suspectes déjà connues et hors périmètre) ; `node --test scripts/lib/doc-graph.test.mjs` → 11/11, aucune régression.
- **Documentation mise à jour** : uniquement `docs/ENGINEERING_BACKLOG.md` §1/§3 (statut M0, prochaine action → TASK-005, explicitement non commencée).
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (aucune décision touchée, contrainte explicite respectée) ; performance/sécurité/accessibilité/TypeScript non applicables (aucun code, aucun contenu de fond modifié) ; tests exécutés (11/11) ; documentation relue.
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : TASK-005 (confirmation explicite que M0 est sorti) est la prochaine Task exploitable — non commencée, conformément à la contrainte explicite de ne pas l'anticiper ; les 6 citations et 54 désynchronisations de version signalées par TASK-002/003 restent ouvertes (hors périmètre de TASK-004, qui portait sur §1 uniquement, pas sur ces défauts déjà classés séparément) ; toutes les décisions produit/UX/visuelles/linguistiques/sensorielles encore ouvertes des phases précédentes le restent.

### TASK-005 — M0 GO/NO-GO (2026-08-05)

- Confirmation explicite de l'utilisateur : « Nous arrivons à la dernière tâche de la Milestone M0. Implémente maintenant uniquement TASK-005. » — revue GO/NO-GO complète demandée sur 13 critères explicites, dépassant le critère de sortie littéral d'un seul point (`docs/MILESTONES.md` §1 : « aucune contradiction non résolue en relecture croisée »). Écart signalé explicitement (pas résolu en silence, même discipline qu'en TASK-004) puis traité comme la spécification effective de cette exécution, à la demande explicite de l'utilisateur.
- Vérifié avant toute conclusion : TASK-001 à TASK-004 terminées (`docs/ENGINEERING_BACKLOG.md` §1/§3) ; aucun ADR en attente ne bloque M0/M1 — recherche exhaustive des mentions « ADR formel/dédié » dans `docs/` : seules deux décisions différées existent (licence MIT, « avant publication » ; stratégie de synchronisation multi-appareils, « avant l'implémentation Phase 2 ») — aucune des deux n'est scopée avant M0 ou M1, aucun blocage réel. `docs/adr/` n'existe pas encore (aucun ADR formel rédigé à ce jour dans le projet, cohérent avec l'absence de décision structurante déjà rencontrée).
- **Revue des 13 critères** (voir compte rendu ci-dessous pour le détail) : 11 Conforme sans réserve, 2 Conforme avec réserve explicite (documentation cohérente : 6 citations de section imprécises + 54 versions d'en-tête désynchronisées, déjà trouvées et tracées par TASK-002/003/004 — ni l'une ni l'autre catégorie ne constitue une **contradiction** au sens du critère officiel de `docs/MILESTONES.md` §1, distinction faite explicitement plutôt que masquée). Aucune non-conformité critique trouvée.
- **Décision : GO.** M0 déclaré officiellement sorti.
- **Aucune nouvelle architecture, règle ou fonctionnalité introduite** — conformément à la contrainte explicite, cette Task ne fait que constater un état déjà produit par TASK-001 à TASK-004.
- **Scripts exécutés** : `node scripts/verify-docs.mjs` → 244 documents, 0 lien cassé, 0 orphelin, 6 citations suspectes déjà connues ; `node --test scripts/lib/doc-graph.test.mjs` → 11/11. Résultats identiques aux exécutions de TASK-004 (aucun contenu modifié entre-temps).
- **Documentation mise à jour** : `docs/ENGINEERING_BACKLOG.md` §1/§3 (M0 marqué sorti, TASK-006/M1 identifiée comme prochaine Task exploitable — **non commencée**, conformément à l'instruction explicite de ne produire aucun code produit).
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (aucune introduite, confirmé) ; performance/sécurité/accessibilité/TypeScript non applicables (aucun code) ; tests exécutés (11/11) ; documentation relue.
- Non commité, non poussé sur `origin/main` à ce stade.
- **Ouvert — clôture officielle de M0** : la Milestone M0 (Architecture validée) est terminée. M1 (Workspace, EPIC-002, TASK-006 première Task) est prête à démarrer mais **non commencée** — aucune Task de M1 engagée, conformément à l'instruction explicite de ne pas l'anticiper. Les 6 citations et 54 désynchronisations de version restent des défauts mineurs connus, non bloquants, non traités par cette Task. Toutes les décisions produit/UX/visuelles/linguistiques/sensorielles encore ouvertes des phases précédentes le restent — elles ne bloquent pas M0/M1 mais devront être tranchées avant les jalons qui en dépendent (`docs/MVP_ROADMAP.md` §4).

### TASK-006 — premier code produit (2026-08-05)

- Confirmation explicite de l'utilisateur : « Nous entrons officiellement dans la Milestone M1... Tu dois implémenter uniquement TASK-006. » — architecture/ADR/standards/documentation déclarés figés, chaque Task désormais implémentée indépendamment.
- Spécification relue intégralement (`docs/TASK_BREAKDOWN.md` §9, gabarit complet TASK-006) : Objectif = déclarer les packages du monorepo pour pnpm ; Documents de référence = `docs/ARCHITECTURE.md` §1, `docs/STACK_DECISIONS.md` §3 ; Dépendances = aucune ; Résultat attendu = `pnpm-workspace.yaml` listant `apps/*`/`packages/*` ; Critère d'acceptation = `pnpm install` sans erreur ; Tests associés = aucun (config pure, vérifiée par `pnpm install` lui-même) ; Documentation impactée = aucune (arborescence déjà dans `ARCHITECTURE.md` §1).
- **ADR** : aucun nécessaire. `docs/STACK_DECISIONS.md` §3 a déjà décidé et justifié pnpm workspaces + Turborepo (comparé à « pas de monorepo » et Nx) en Phase 0.5 — TASK-006 applique cette décision, n'en introduit aucune nouvelle (cohérent avec le propre champ « Risques » de la Task : « Aucun — Task triviale, aucune décision technique nouvelle »).
- **Prérequis vérifié et gap réel trouvé** : Node présent (v22.14.0), Git repo confirmé, mais **pnpm n'était pas installé localement** — écart avec le prérequis explicite de la Task. `corepack enable`/`corepack prepare pnpm@latest --activate` a échoué (`EPERM`, écriture dans `C:\Program Files\nodejs\` nécessitant des droits administrateur non disponibles dans cet environnement). Contourné sans installation globale ni action système invasive : `npx --yes pnpm@latest` (pnpm 11.20.0), qui n'exige aucun droit élevé et ne modifie aucun état global de la machine.
- **Résultat** : `pnpm-workspace.yaml` créé à la racine du dépôt (`packages: ["apps/*", "packages/*"]`), conforme à l'arborescence déjà actée (`docs/ARCHITECTURE.md` §1).
- **Vérification empirique, pas supposée** : hypothèse initiale qu'un `package.json` racine serait aussi nécessaire pour que `pnpm install` réussisse — testée directement plutôt qu'assumée. Résultat réel : `pnpm install` réussit (code de sortie 0, « No projects found », aucun `node_modules` créé) avec `pnpm-workspace.yaml` seul, sans `package.json`. Aucun fichier supplémentaire créé sur la base d'une supposition non vérifiée.
- **Aucun autre fichier créé ou modifié** — pas de `package.json`, pas de dossiers `apps/`/`packages/` (hors périmètre : TASK-008 et TASK-009 à 012 respectivement), pas de `.gitignore` modifié (aucun `node_modules` généré, rien à ignorer pour l'instant).
- **Tests** : aucun test créé — conforme au propre champ de la Task (« Tests associés : Aucun »), cohérent avec `docs/TESTING_STRATEGY.md` §9septies : un fichier de configuration déclaratif sans logique n'a rien à tester unitairement, sa correction est vérifiée par l'outil qui le consomme. Suite existante (`scripts/lib/doc-graph.test.mjs`, 11 tests) et `scripts/verify-docs.mjs` réexécutés comme garde-fou de non-régression — aucune régression, aucun changement de contenu documentaire.
- **Documentation mise à jour** : uniquement `docs/ENGINEERING_BACKLOG.md` §1/§3 (M1 en cours, TASK-006 terminée, prochaine action → TASK-007) — `docs/ARCHITECTURE.md` explicitement non modifié (« Documentation impactée : Aucune » déjà acté par la Task elle-même, arborescence déjà décrite).
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (conforme à `ARCHITECTURE.md` §1, aucune déviation) ; TypeScript/Lint (non applicable — aucun outillage configuré à ce stade, ce sera TASK-013/014) ; Build (non applicable — aucun bundler configuré, TASK-007+) ; Tests (11/11, aucune régression) ; sécurité (non applicable, fichier de configuration statique sans donnée sensible) ; performance (non applicable) ; documentation relue.
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : TASK-007 (configurer `turbo.json`) est la prochaine Task exploitable — non commencée, conformément à l'instruction explicite de ne pas l'anticiper. **Limite d'environnement à noter** : pnpm reste accessible uniquement via `npx` sur cette machine, pas installé globalement (droits administrateur requis pour `corepack enable` sur ce Windows) — les Tasks suivantes qui invoquent `pnpm` devront soit répéter ce contournement, soit qu'un opérateur avec droits élevés active corepack une fois pour toutes.

### TASK-007 (2026-08-05)

- Confirmation explicite de l'utilisateur : « Tu dois maintenant implémenter uniquement TASK-007. » — architecture/ADR/standards figés confirmés comme source de vérité, périmètre strict, pas d'anticipation de TASK-008.
- Spécification lue (`docs/TASK_BREAKDOWN.md` §6 : « Configurer `turbo.json` (pipeline `build`/`lint`/`test`/`dev`) », S, dépend de TASK-006). Aucun gabarit détaillé à 14 champs pour cette Task (hors des trois exemplaires de §9) — contexte dérivé de FEATURE-003 (STORY-003, même Story que TASK-006/008), `docs/ARCHITECTURE.md` §1 (turbo.json à la racine), `docs/STACK_DECISIONS.md` §3 (décision Turborepo déjà actée et justifiée), `docs/CI_CD_GUIDE.md` §1 (jobs orchestrés par Turborepo, confirmant l'usage attendu sans en élargir le périmètre), `docs/RISK_REGISTER_TECHNICAL.md` §1 (risque de cache Turborepo déjà couvert par le hash de contenu par défaut, aucune config supplémentaire requise).
- **ADR** : aucun nécessaire — même raisonnement que TASK-006, `STACK_DECISIONS.md` §3 a déjà décidé et comparé Turborepo (vs pas de monorepo, vs Nx). Configurer `turbo.json` applique cette décision, n'en prend aucune nouvelle.
- TASK-006 revérifiée avant de commencer : `pnpm-workspace.yaml` toujours présent, `pnpm install` toujours sans erreur (code 0).
- **Aucune ambiguïté bloquante trouvée** — la forme exacte des tâches `build`/`lint`/`test`/`dev` (dependsOn, outputs, cache) n'est spécifiée nulle part dans `docs/`, mais c'est un détail d'implémentation standard de l'outil déjà choisi, pas une décision structurante (cohérent avec `docs/DEVELOPER_PLAYBOOK.md` §3.4) — appliqué selon les conventions officielles connues de Turborepo plutôt qu'inventé, jamais un STOP nécessaire pour ce niveau de détail.
- **Résultat** :
  - `package.json` (racine, minimal : `name`, `private`, `packageManager`) — **créé, pas dans le périmètre littéral de TASK-006, nécessaire ici** : `turbo` doit être installable comme devDependency, ce qui exige un manifeste racine ; `pnpm install` seul (TASK-006) n'en avait pas eu besoin (vérifié empiriquement à l'époque), mais `pnpm add` en a structurellement besoin.
  - `turbo` installé en devDependency racine via `pnpm add -D turbo -w` (contournement `npx pnpm`, même limite d'environnement que TASK-006) — version résolue 2.10.8.
  - `turbo.json` (racine) — schéma v2 (`"tasks"`, pas l'ancien `"pipeline"` de v1, cohérent avec la version réellement installée) : `build` (`dependsOn: ["^build"]`, `outputs: ["dist/**"]`), `lint` (`dependsOn: ["^build"]`), `test` (`dependsOn: ["^build"]`), `dev` (`cache: false`, `persistent: true`) — les 4 tâches exactement nommées par le titre de la Task, aucune tâche supplémentaire (`typecheck`/`test:unit`/etc. de `CI_CD_GUIDE.md` §1 délibérément non ajoutées ici, hors périmètre de TASK-007).
  - `.gitignore` étendu (`node_modules/`, `.turbo/`) — **modification externe justifiée** : l'installation de `turbo` crée nécessairement ces deux répertoires, jamais destinés à être committés (hygiène de dépôt standard, pas une nouvelle règle inventée).
- **Validation empirique de chaque tâche** (pas seulement une lecture du schéma) : `turbo build --dry-run`, `turbo lint --dry-run`, `turbo test --dry-run`, `turbo dev --dry-run` exécutés individuellement — les 4 réussissent (code 0), « Running build in 0 packages » cohérent avec l'absence encore attendue de `apps/`/`packages/` (TASK-008/009-012).
- **Sécurité** : `pnpm audit` exécuté sur la dépendance nouvellement ajoutée — 0 vulnérabilité connue.
- **Tests** : aucun test unitaire créé — configuration déclarative sans logique propre, vérifiée par l'outil lui-même (même raisonnement que TASK-006). Suite existante réexécutée : `node --test scripts/lib/doc-graph.test.mjs` → 11/11 ; `node scripts/verify-docs.mjs` → 244 documents, 0 lien cassé, 0 orphelin — aucune régression.
- **Documentation mise à jour** : uniquement `docs/ENGINEERING_BACKLOG.md` §1/§3 — `docs/ARCHITECTURE.md` non modifié (turbo.json déjà mentionné dans son arborescence §1, rien à ajouter).
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (conforme, `turbo.json` au niveau documenté) ; TypeScript/ESLint (non applicable, aucun outillage configuré avant TASK-013/014) ; Build (validé par dry-run, 0 package à builder pour l'instant, cohérent) ; Tests (11/11) ; performance (non applicable) ; sécurité (`pnpm audit` clean) ; accessibilité (non applicable, aucune UI) ; documentation relue.
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : TASK-008 (créer l'arborescence `apps/` vide) est la prochaine Task exploitable — non commencée, conformément à l'instruction explicite de ne pas l'anticiper. Turborepo collecte une télémétrie anonyme propre à l'outil (pas liée au produit Melodia ni à ses utilisateurs) — observé mais non désactivé, cette Task ne couvrait pas la configuration d'environnement CI/local au-delà de `turbo.json` lui-même ; signalé ici comme point à considérer pour une future Task de configuration CI plutôt que traité en silence ou anticipé sans mandat.

### TASK-008 (2026-08-05)

- Message reçu sous forme d'un gabarit réutilisable (« IMPLEMENTATION WORKFLOW ») avec un `{{TASK_ID}}` non substitué. Plutôt que deviner en silence, une clarification a été proposée via une question structurée — rejetée par l'outil faute d'une seconde option réellement distincte (un seul candidat crédible existait : TASK-009 n'était pas encore débloquée pour être « la » tâche évidente, mais TASK-008 était explicitement la prochaine action recommandée à la fin de TASK-007 et la seule Task non bloquée du backlog). Poursuivi avec TASK-008, annoncé explicitement avant d'agir plutôt que silencieusement.
- Spécification lue (`docs/TASK_BREAKDOWN.md` §6 : « Créer l'arborescence `apps/` (web/desktop/mobile) vide », XS, dépend de TASK-006). Documents référencés : `docs/ARCHITECTURE.md` §1 (arborescence faisant autorité). TASK-006 revérifiée : dépendance satisfaite.
- **Analyse présentée avant tout code** (étape explicitement exigée par le gabarit de l'utilisateur, nouvelle par rapport aux Tasks précédentes) : objectif, périmètre (exclusion explicite de `vite.config.ts`/`src/main.tsx`/`src-tauri/`/`tauri.conf.json` — le titre dit « vide », aucune Task décomposée à ce jour ne couvre leur contenu), fichiers probables, risques — validée implicitement en poursuivant vers l'implémentation.
- **ADR** : aucun nécessaire — réalisation mécanique d'une arborescence déjà entièrement actée dans `ARCHITECTURE.md` §1.
- **Résultat** : `apps/web/.gitkeep`, `apps/desktop/.gitkeep`, `apps/mobile/.gitkeep` — Git ne suit pas les répertoires vides, un placeholder par dossier est la convention standard (pas une décision d'architecture). Aucun fichier de configuration interne créé (hors périmètre, voir ci-dessus).
- **Tests** : aucun test créé — trois répertoires vides n'ont aucune logique à tester. Suite existante réexécutée : `node --test scripts/lib/doc-graph.test.mjs` → 11/11 ; `node scripts/verify-docs.mjs` → 244 documents, 0 lien cassé, 0 orphelin ; `pnpm install` et `turbo build --dry-run` revérifiés après création des dossiers → toujours code 0, aucune régression (le comportement de `pnpm install` change légèrement — « Already up to date » au lieu de « No projects found » — cohérent avec l'existence du `package.json` racine depuis TASK-007, pas un effet de TASK-008 elle-même).
- **Documentation mise à jour** : uniquement `docs/ENGINEERING_BACKLOG.md` §1/§3 — `docs/ARCHITECTURE.md` non modifié (arborescence déjà décrite intégralement).
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (conforme à `ARCHITECTURE.md` §1) ; TypeScript/ESLint/Build (non applicables, aucun contenu de package dans ces dossiers) ; Tests (11/11) ; performance/sécurité/accessibilité (non applicables, aucun code) ; documentation relue.
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : TASK-009 (squelette `@melodia/core`, dépend de TASK-007) est la prochaine Task exploitable — non commencée, conformément à l'instruction explicite de ne pas anticiper. `packages/*` (destinataire de TASK-009 à 012) reste entièrement non créé, cohérent avec le périmètre strict de TASK-008 (uniquement `apps/`).

### TASK-009 (2026-08-05)

- Confirmation explicite de l'utilisateur : « Implémente uniquement TASK-008 en suivant strictement le workflow officiel. » — **incohérence détectée avant toute action** : TASK-008 était déjà terminée (vérifié : `apps/web`/`apps/desktop`/`apps/mobile` existants, `docs/ENGINEERING_BACKLOG.md` et cette même entrée de journal l'enregistrant comme close, TASK-009 déjà recommandée comme suite). Signalé explicitement à l'utilisateur via une question structurée plutôt que ré-exécuté en silence ou ignoré silencieusement — l'utilisateur a confirmé « Passer à TASK-009 ».
- Spécification lue (`docs/TASK_BREAKDOWN.md` §6, FEATURE-004/STORY-004 : « Squelette `@melodia/core` » — `package.json`/tsconfig/dossiers `entities`/`data`/`search` vides, S, dépend de TASK-007). Documents référencés : `docs/ARCHITECTURE.md` §1-2 (arborescence et frontières de `@melodia/core` déjà actées), `docs/TECH_STACK.md` §1 (TypeScript strict). TASK-007 revérifiée intacte (`pnpm install` → code 0).
- **ADR** : aucun nécessaire — `@melodia/core` et ses sous-dossiers sont déjà entièrement décidés dans `ARCHITECTURE.md` §1-2, aucune décision nouvelle.
- **Résultat** :
  - `packages/core/package.json` — `{ "name": "@melodia/core", "version": "0.0.0", "private": true, "type": "module" }`. **Aucun champ `main`/`exports`/`scripts`** — décision délibérée, pas une omission : aucun fichier source n'existe encore (`entities`/`data`/`search` vides par exigence explicite du titre de la Task), inventer un point d'entrée ou des scripts de build maintenant aurait été une improvisation non demandée.
  - `packages/core/tsconfig.json` — config TypeScript strict autonome (`strict: true`, `target: ES2022`, `module: ESNext`, `moduleResolution: Bundler`, `declaration: true`) cohérente avec `TECH_STACK.md` §1. N'étend pas de `tsconfig.base.json` — ce fichier n'existe pas encore (TASK-013, hors périmètre, non anticipée) ; ce tsconfig sera refactorisé pour l'étendre quand TASK-013 sera faite, pas avant.
  - `packages/core/src/entities/.gitkeep`, `packages/core/src/data/.gitkeep`, `packages/core/src/search/.gitkeep` — dossiers vides, mêmes conventions que TASK-008.
- **Validation empirique** (pas supposée) : `pnpm install` découvre désormais `@melodia/core` comme membre réel du workspace (`pnpm -r list --depth -1` le confirme) ; les 4 tâches Turborepo (`build`/`lint`/`test`/`dev`) ciblent correctement le package en dry-run ; `turbo build` réel exécuté : code 0 avec `WARNING No tasks were executed` (`package.json` sans script `build`, comportement Turborepo attendu et correct, pas une erreur).
- **Sécurité** : `pnpm audit` → 0 vulnérabilité.
- **Tests** : aucun test créé — trois dossiers vides et deux fichiers de configuration déclaratifs n'ont rien à tester unitairement. Suite existante réexécutée : `node --test scripts/lib/doc-graph.test.mjs` → 11/11 ; `node scripts/verify-docs.mjs` → 244 documents, 0 lien cassé, 0 orphelin — aucune régression.
- **Documentation mise à jour** : uniquement `docs/ENGINEERING_BACKLOG.md` §1/§3 — `docs/ARCHITECTURE.md` non modifié (squelette déjà décrit intégralement).
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (conforme à `ARCHITECTURE.md` §2, `@melodia/core` ne dépend de rien d'interne au projet à ce stade) ; typage (strict activé, cohérent avec `TECH_STACK.md` §1) ; build/lint (validés par `turbo --dry-run`, 0 script à date, cohérent) ; tests (11/11) ; sécurité (`pnpm audit` clean) ; performance (non applicable, aucun code exécutable) ; documentation relue.
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : TASK-010 (squelette `@melodia/ui`, dépend de TASK-007) est la prochaine Task exploitable — non commencée, conformément à l'instruction explicite de ne pas anticiper.

### TASK-010 (2026-08-05)

- **Même incohérence qu'au tour précédent, cette fois répétée à l'identique** : le message redemandait TASK-008, déjà terminée depuis deux tours, avec TASK-009 également terminée entre-temps — 2e occurrence du même modèle de message probablement réutilisé sans mise à jour du numéro de tâche. Signalé explicitement à nouveau (avec mention du caractère répété, pour que l'utilisateur puisse corriger le modèle source) plutôt que ré-exécuté ou ignoré en silence — confirmé « Passer à TASK-010 ».
- Spécification lue (`docs/TASK_BREAKDOWN.md` §6, FEATURE-004/STORY-005 : « Squelette `@melodia/ui` » — idem TASK-009, dossier `components/` vide, S, dépend de TASK-007). Documents référencés : `docs/ARCHITECTURE.md` §1 (`packages/ui/src/components/`) et §2 (frontières : `@melodia/ui` ne dépend d'aucun package interne, uniquement Radix/Tailwind — respecté par construction, aucune dépendance workspace ajoutée). TASK-007 revérifiée intacte.
- **ADR** : aucun nécessaire — `@melodia/ui` déjà entièrement décidé dans `ARCHITECTURE.md` §1-2.
- **Résultat** :
  - `packages/ui/package.json` — même gabarit minimal que `@melodia/core` (`name`, `version`, `private`, `type`), aucun `main`/`exports`/`scripts` (aucun fichier source encore).
  - `packages/ui/tsconfig.json` — même config stricte autonome que `@melodia/core`, **plus `"jsx": "react-jsx"`** : ajout délibéré et justifié (pas une improvisation) — `ARCHITECTURE.md` décrit explicitement `@melodia/ui` comme le design system de composants React (« composants purement présentationnels »), et React 19 est déjà acté projet-large (`TECH_STACK.md` §1) ; omettre `jsx` maintenant aurait été une omission qui casserait la compilation dès le premier composant `.tsx` ajouté, pas une neutralité.
  - `packages/ui/src/components/.gitkeep` — dossier vide, même convention que TASK-008/009.
- **Validation empirique** : `pnpm install` découvre `@melodia/ui` (3 packages workspace au total avec `@melodia/core`) ; `turbo build` réel → code 0, `WARNING No tasks were executed` (cohérent, aucun script).
- **Sécurité** : `pnpm audit` → 0 vulnérabilité.
- **Tests** : aucun test créé — même raisonnement que TASK-009 (dossiers vides, configuration déclarative). Suite existante réexécutée : `node --test scripts/lib/doc-graph.test.mjs` → 11/11 ; `node scripts/verify-docs.mjs` → 244 documents, 0 lien cassé, 0 orphelin — aucune régression.
- **Documentation mise à jour** : uniquement `docs/ENGINEERING_BACKLOG.md` §1/§3 — `docs/ARCHITECTURE.md` non modifié.
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (conforme à `ARCHITECTURE.md` §2, aucune dépendance interne ajoutée à `@melodia/ui`, respecte la frontière « ne doit jamais dépendre de `@melodia/core`/`@melodia/app` ») ; typage (strict + JSX, cohérent avec le rôle documenté du package) ; build/lint (validés, 0 script à date) ; tests (11/11) ; sécurité (`pnpm audit` clean) ; performance (non applicable) ; documentation relue.
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : TASK-011 (squelette `@melodia/platform`, dépend de TASK-007) est la prochaine Task exploitable — non commencée, conformément à l'instruction explicite de ne pas anticiper.

### TASK-011 (2026-08-05)

- Instruction explicite et sans ambiguïté cette fois (« commence la task 11 ») — pas de discordance à signaler, contrairement aux deux tours précédents.
- Spécification lue (`docs/TASK_BREAKDOWN.md` §6, FEATURE-004/STORY-006 : « Squelette `@melodia/platform` » — idem, XS, dépend de TASK-007). Documents référencés : `docs/ARCHITECTURE.md` §1 (`packages/platform/src/`, aucun sous-dossier nommé contrairement à `core`/`ui`) et §2 (frontières : `@melodia/platform` ne dépend de rien d'interne, et rien ne doit en dépendre dans le mauvais sens). TASK-007 revérifiée intacte.
- **ADR** : aucun nécessaire — déjà entièrement décidé dans `ARCHITECTURE.md` §1-2.
- **Résultat** :
  - `packages/platform/package.json` — même gabarit minimal.
  - `packages/platform/tsconfig.json` — même config stricte que `@melodia/core`, **sans** `jsx` (contrairement à `@melodia/ui`) : `ARCHITECTURE.md` §1 décrit `@melodia/platform` comme une abstraction Tauri/navigateur (fenêtrage, notifications, fichiers), pas des composants React — ajouter `jsx` ici aurait été une improvisation non justifiée par la documentation.
  - `packages/platform/src/.gitkeep` — un seul dossier `src/`, cohérent avec l'absence de sous-dossier nommé dans `ARCHITECTURE.md` §1 pour ce package (à la différence de `core`/`ui`).
- **Validation empirique** : `pnpm install` découvre `@melodia/platform` (4 projets workspace au total : racine + `core` + `ui` + `platform`) ; `turbo build` réel → code 0, `WARNING No tasks were executed` (cohérent).
- **Sécurité** : `pnpm audit` → 0 vulnérabilité.
- **Tests** : aucun test créé — même raisonnement que TASK-009/010. Suite existante réexécutée : `node --test scripts/lib/doc-graph.test.mjs` → 11/11 ; `node scripts/verify-docs.mjs` → 244 documents, 0 lien cassé, 0 orphelin — aucune régression.
- **Documentation mise à jour** : uniquement `docs/ENGINEERING_BACKLOG.md` §1/§3.
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (conforme, aucune dépendance ajoutée) ; typage (strict, sans JSX injustifié) ; build/lint (validés, 0 script à date) ; tests (11/11) ; sécurité (`pnpm audit` clean) ; performance (non applicable) ; documentation relue.
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : TASK-012 (squelette `@melodia/app`, dépend de TASK-007) est la prochaine Task exploitable — non commencée, conformément à l'instruction explicite de ne pas anticiper.

### TASK-012 (2026-08-05)

- Instruction explicite (« go task 12 »).
- Spécification lue (`docs/TASK_BREAKDOWN.md` §6, FEATURE-004/STORY-007 : « Squelette `@melodia/app` » — dossiers `app`/`features`/`shared`/`data`/`platform` vides, cite [[CODING_STANDARDS.md]] §1, S, dépend de TASK-007). TASK-007 revérifiée intacte.
- **Incohérence d'architecture réelle détectée et signalée avant implémentation, pas résolue en silence** : le titre de TASK-012 liste littéralement `data`/`platform` parmi les dossiers de `@melodia/app` — c'est l'héritage du `src/` unique de la Phase 0 (`CODING_STANDARDS.md` §1, avant le monorepo), jamais mis à jour dans le titre de la Task quand `ARCHITECTURE.md` §1-2 a ensuite déplacé concrètement `data/` dans `packages/core/src/` (déjà créé par TASK-009) et `platform/` dans `packages/platform/` (déjà créé par TASK-011, package séparé entier). Créer `data/`/`platform/` sous `packages/app/src/` aurait dupliqué le territoire de deux packages déjà implémentés dans ce même backlog et violé `ARCHITECTURE.md` §2 (`@melodia/app` *dépend* de `core`/`ui`/`platform`, ne réimplémente jamais leur contenu). **Aucun ADR nécessaire** — aucune nouvelle décision, application de celle déjà actée et déjà exécutée par TASK-009/011 ; l'écart est une imprécision de titre dans `TASK_BREAKDOWN.md`, pas une question d'architecture ouverte. `TASK_BREAKDOWN.md` non modifié (hors périmètre de TASK-012, correction opportuniste explicitement proscrite) — écart uniquement signalé, ici et dans `docs/ENGINEERING_BACKLOG.md` §3.
- **Résultat** : `packages/app/package.json` (gabarit minimal identique) ; `packages/app/tsconfig.json` (strict + `jsx: react-jsx`, `@melodia/app` étant l'application React elle-même) ; `packages/app/src/app/.gitkeep`, `packages/app/src/features/.gitkeep`, `packages/app/src/shared/.gitkeep` — **3 dossiers, pas 5**, `data`/`platform` délibérément omis pour la raison ci-dessus.
- **Validation empirique** : `pnpm install` découvre les 4 packages du workspace (`app`, `core`, `platform`, `ui`) — `FEATURE-004` complète ; `turbo build` réel → code 0, `WARNING No tasks were executed` (cohérent, aucun script encore).
- **Sécurité** : `pnpm audit` → 0 vulnérabilité.
- **Tests** : aucun test créé — même raisonnement que TASK-009/010/011. Suite existante réexécutée : `node --test scripts/lib/doc-graph.test.mjs` → 11/11 ; `node scripts/verify-docs.mjs` → 244 documents, 0 lien cassé, 0 orphelin — aucune régression.
- **Documentation mise à jour** : uniquement `docs/ENGINEERING_BACKLOG.md` §1/§3 (incohérence signalée dans le texte, `TASK_BREAKDOWN.md`/`ARCHITECTURE.md` non modifiés).
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (conforme à `ARCHITECTURE.md` §1-2, écart de titre signalé et non reproduit dans le code) ; typage (strict + JSX justifié) ; build/lint (validés, 0 script à date) ; tests (11/11) ; sécurité (`pnpm audit` clean) ; performance (non applicable) ; documentation relue.
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : TASK-013 (`tsconfig.base.json`, dépend de TASK-009 à 012, toutes désormais satisfaites) est la prochaine Task exploitable — non commencée, conformément à l'instruction explicite de ne pas anticiper. Les 4 `tsconfig.json` de packages créés à ce jour (core/ui/platform/app) restent autonomes, à refactoriser pour étendre `tsconfig.base.json` une fois TASK-013 faite — pas avant, pour ne pas l'anticiper.

### TASK-013 (2026-08-05)

- Instruction explicite (« passe a la task 13 »).
- Spécification lue (`docs/TASK_BREAKDOWN.md` §6, FEATURE-005/STORY-008 : « `tsconfig.base.json` (strict activé, [[TECH_STACK.md]] §1) », XS, dépend de TASK-009 à 012 — toutes revérifiées terminées). Document de référence relu attentivement : `TECH_STACK.md` §1 précise « `strict: true` intégral, `noUncheckedIndexedAccess` activé » — **gap réel trouvé** : `noUncheckedIndexedAccess` manquait dans les 4 `tsconfig.json` déjà créés (TASK-009 à 012), jamais signalé jusqu'ici faute d'avoir relu ce document précis à ce niveau de détail à l'époque.
- **ADR** : aucun nécessaire — `packages/config` et le mode strict sont déjà décidés (`ARCHITECTURE.md` §3, `TECH_STACK.md` §1).
- **Extension de périmètre justifiée, annoncée avant exécution** : au-delà de la création de `tsconfig.base.json` elle-même, les 4 `tsconfig.json` déjà créés (core/ui/platform/app) ont été mis à jour pour l'étendre — un fichier de base jamais consommé par personne n'aurait eu aucun effet réel, et aucune autre Task du backlog décomposé ne réclame ce câblage. Ce n'est pas une anticipation de TASK-014+ : ces 4 fichiers sont directement causés par ce que TASK-013 produit, pas des composants indépendants hors périmètre.
- **Résultat** :
  - `packages/config/package.json` (`@melodia/config`, gabarit minimal).
  - `packages/config/tsconfig.base.json` — `strict: true`, `noUncheckedIndexedAccess: true` (gap comblé), `target/lib/module/moduleResolution/esModuleInterop/skipLibCheck/forceConsistentCasingInFileNames/declaration` communs aux 4 packages.
  - `packages/{core,ui,platform,app}/package.json` — ajout de `@melodia/config` en devDependency (`workspace:*`), nécessaire pour que pnpm crée le lien symbolique permettant la résolution `"extends": "@melodia/config/tsconfig.base.json"`.
  - `packages/{core,ui,platform,app}/tsconfig.json` — réduits à `extends` + overrides propres au package (`jsx` pour ui/app) + `include`.
  - `typescript` installé en devDependency racine (`pnpm add -D typescript -w`, résolu en 7.0.2) — sans le compilateur, « strict activé » n'a rien à faire respecter ; même raisonnement que l'installation de `turbo` en TASK-007.
- **Bug réel trouvé et corrigé par validation empirique, pas supposé correct** : `tsc --showConfig` dans `packages/core` a révélé que `outDir`/`rootDir` définis dans la base se résolvaient en `../config/dist`/`../config/src` — TypeScript résout les chemins relatifs d'une config étendue par rapport à l'emplacement du fichier où ils sont *définis* (`packages/config`), pas à celui qui étend. Corrigé en retirant `outDir`/`rootDir` de la base et en les redéfinissant dans chacun des 4 `tsconfig.json` de package. Revérifié après correction : les 4 packages résolvent désormais correctement leurs propres chemins.
- **Validation empirique complète** : `tsc --showConfig` dans les 4 packages confirme `strict`/`noUncheckedIndexedAccess`/`jsx` correctement hérités et `outDir`/`rootDir` correctement locaux. `tsc --noEmit` échoue avec `TS18003 No inputs were found` dans les 4 — attendu et correct, aucun fichier `.ts` réel n'existe encore (seulement des `.gitkeep`), pas un défaut de configuration.
- **Sécurité** : `pnpm audit` → 0 vulnérabilité (`typescript` inclus).
- **Tests** : aucun test créé — configuration déclarative. Suite existante réexécutée : `node --test scripts/lib/doc-graph.test.mjs` → 11/11 ; `node scripts/verify-docs.mjs` → 244 documents, 0 lien cassé, 0 orphelin ; `turbo build` → code 0, aucune régression.
- **Documentation mise à jour** : uniquement `docs/ENGINEERING_BACKLOG.md` §1/§3.
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (conforme à `ARCHITECTURE.md` §3) ; typage (strict + `noUncheckedIndexedAccess` désormais réellement conforme à `TECH_STACK.md` §1 sur les 4 packages, gap comblé) ; build (`turbo build` clean) ; lint (non applicable, TASK-014) ; tests (11/11) ; sécurité (`pnpm audit` clean) ; performance (non applicable) ; documentation relue.
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : TASK-014 (ESLint partagé, dépend de TASK-013) est la prochaine Task exploitable — non commencée, conformément à l'instruction explicite de ne pas anticiper.

### TASK-014 (2026-08-05)

- Instruction explicite (« go a la TASK 14 »). Spécification lue intégralement (`docs/TASK_BREAKDOWN.md` §9, gabarit complet à 14 champs — un des trois exemplaires entièrement détaillés du backlog). Documents de référence lus : `docs/CODING_STANDARDS.md` §3 (ordre d'import), `docs/ENGINEERING_MANIFESTO.md` §2 (`no-explicit-any` comme référence de rigueur bloquante), `docs/TYPESCRIPT_GUIDE.md` §8 (`any` interdit sauf justification, `unknown` par défaut), `docs/ACCESSIBILITY_GUIDE.md` §9ter (`eslint-plugin-jsx-a11y`, règles citées : `alt-text`, `role-has-required-aria-props`, `no-noninteractive-element-interactions`). TASK-013 revérifiée terminée avant de commencer.
- **ADR** : aucun nécessaire — les règles à activer sont déjà toutes actées dans les documents ci-dessus, `packages/config` comme point de câblage déjà décidé (`ARCHITECTURE.md` §3).
- **Conflit de versions réel trouvé et résolu avant d'aller plus loin** : l'installation initiale (`eslint@10.8.0`, `typescript-eslint@8.66.0`, `eslint-plugin-import@2.32.0`, `eslint-plugin-jsx-a11y@6.10.2`) a produit des avertissements `pnpm peers check` réels — `typescript-eslint` exige `typescript <6.1.0` (nous avions `7.0.2`, installé en TASK-013), et `eslint-plugin-import`/`eslint-plugin-jsx-a11y` exigent `eslint <=9` (nous avions `10.8.0`). Plutôt que d'ignorer l'avertissement ou de tester à l'aveugle une combinaison hors plage déclarée — risque explicitement nommé par la Task elle-même (« risque fondation ») — repincé à des versions mutuellement compatibles : `eslint@9.39.5`, `@eslint/js@9`, `typescript@6.0.3`. **TASK-013 revérifiée après coup** : `tsc --showConfig` échouait désormais avec `TS18003` là où il réussissait auparavant (différence de comportement réelle entre TypeScript 7 et 6.0.3 face à un dossier `src/` sans fichier réel, pas une régression de configuration) — validé en créant un fichier `.ts` temporaire, confirmant que `strict`/`noUncheckedIndexedAccess`/`outDir`/`rootDir` restent corrects, puis supprimé.
- **Résultat** :
  - `packages/config/eslint-preset.js` — flat config ESLint 9 : `js.configs.recommended` + `tseslint.configs.recommended` + `jsxA11y.flatConfigs.recommended` + `@typescript-eslint/no-explicit-any` en erreur, `@typescript-eslint/consistent-type-imports` (`fixStyle: separate-type-imports`, auto-fixable — répond à l'exigence « imports de type toujours séparés » de `TYPESCRIPT_GUIDE.md` §8), les 3 règles `jsx-a11y` explicitement citées en erreur, `import/order` avec `pathGroups` ordonnés `@platform → @data → @entities → @shared → @features → @app` (interprétation justifiée de « de @data vers @app », l'ordre intermédiaire exact n'étant pas explicité dans `CODING_STANDARDS.md` §3 — signalé plutôt que deviné en silence), `eslint-plugin-boundaries` enregistré comme plugin sans aucune règle `boundaries/*` active (préparé, pas activé — conforme au critère d'acceptation).
  - **Écart volontaire du preset "recommended" d'`eslint-plugin-import`** : celui-ci active `import/no-unresolved`, dont le résolveur par défaut ne comprend pas la convention d'extension `.js`→`.ts` de ce monorepo — testé empiriquement, faux positif confirmé sur un import relatif valide. Plutôt qu'installer `eslint-import-resolver-typescript` (dépendance et rule set non demandés par TASK-014, qui ne réclame que l'ordre d'import), le plugin est enregistré manuellement avec uniquement `import/order` activé — périmètre strictement aligné sur ce qui est demandé.
  - `packages/config/package.json` — ajout des dépendances ESLint (`@eslint/js`, `typescript-eslint`, `eslint-plugin-jsx-a11y`, `eslint-plugin-import`, `eslint-plugin-boundaries`).
  - `packages/{core,ui,platform,app}/package.json` — ajout du script `"lint": "eslint ."` et de la devDependency `eslint@9.39.5`.
  - `packages/{core,ui,platform,app}/eslint.config.js` — chacun réexporte le preset partagé.
- **Validation empirique complète, pas supposée** (fichiers temporaires créés puis systématiquement supprimés, jamais commités) : `no-explicit-any` bloque un fichier `any`/`any` (2 erreurs, code 1) ; `import/order` détecte un ordre inversé ET le corrige correctement via `--fix` (vérifié avant/après) ; `jsx-a11y/alt-text` bloque une balise `<img>` sans `alt` dans `packages/ui` (JSX actif, code 1). `turbo lint` exécuté sur les 4 packages sur le code réel (vide) du dépôt : 4/4 réussis, 0 erreur.
- **Sécurité** : `pnpm audit` → 0 vulnérabilité.
- **Tests** : aucun test automatisé propre créé — conforme au champ « Tests associés » de la Task elle-même (vérifié par l'exécution du lint sur des fichiers fautifs volontaires, déjà fait ci-dessus). Suite existante réexécutée : `node --test scripts/lib/doc-graph.test.mjs` → 11/11 ; `node scripts/verify-docs.mjs` → 244 documents, 0 lien cassé, 0 orphelin ; `turbo build` → toujours 0 tâche exécutée, cohérent.
- **Documentation mise à jour** : uniquement `docs/ENGINEERING_BACKLOG.md` §1/§3 — conforme au champ « Documentation impactée : Aucune » de la Task (les règles étaient déjà actées, cette Task les active).
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (conforme, aucune frontière touchée) ; typage (cohérent avec `TYPESCRIPT_GUIDE.md` §8) ; build (`turbo build` clean) ; lint (4/4 packages verts, règles requises validées empiriquement) ; tests (11/11) ; sécurité (`pnpm audit` clean, conflit de versions résolu avant merge) ; performance (non applicable) ; accessibilité (jsx-a11y validé) ; documentation relue.
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : TASK-015 (Preset Tailwind partagé, dépend de TASK-013) est la prochaine Task exploitable — non commencée, conformément à l'instruction explicite de ne pas anticiper. `eslint-import-resolver-typescript` reste un gap connu si `import/no-unresolved` devait être activé plus tard — non traité ici, hors périmètre.

### TASK-015 — arrêtée sur ADR-0001 (2026-08-05)

- Instruction explicite (« tu peut passer a la task 15 »). Spécification lue (`docs/TASK_BREAKDOWN.md` §6, FEATURE-005/STORY-008 : « Preset Tailwind partagé », S, dépend de TASK-013 — revérifiée intacte). Documents de référence lus : `docs/DESIGN_SYSTEM_ARCHITECTURE.md` §1 (mécanisme des tokens statiques), `docs/DESIGN_TOKENS.md` (cadrage, catégories), `docs/ARCHITECTURE.md` §3 (`packages/config` fournit le preset Tailwind).
- **Incohérence d'architecture réelle détectée, pas une simple imprécision de titre cette fois** : `docs/DESIGN_SYSTEM_ARCHITECTURE.md` §1 décrit le mécanisme comme « extension du thème Tailwind (`tailwind.config`) » — le mécanisme JS de Tailwind CSS v3 (fichier `tailwind.config.js`, tableau `presets`). Installation exploratoire de `tailwindcss` (dernière version du registre, 4.3.3) pour vérifier l'API réelle avant d'écrire le preset : ses exports (`index.css`, `theme.css`, `preflight.css`, `utilities.css`, `plugin.js`, `defaultTheme.js`, `colors.js`) confirment que Tailwind v4 n'expose plus de chargeur de config JS — le thème s'étend nativement en CSS via des blocs `@theme`. Une compatibilité ascendante existe (directive `@config`) mais documentée par l'outil lui-même comme un pont de migration, pas le chemin recommandé pour un nouveau projet.
- **STOP appliqué conformément à l'instruction explicite de l'utilisateur** (« Si une incohérence d'architecture est détectée : STOP. Ne rien implémenter. Créer uniquement un ADR. ») : `docs/adr/0001-tailwind-v4-theme-mechanism.md` créé (premier ADR formel du projet, `docs/adr/` n'existait pas encore) — 3 options comparées (compat `@config` v3→v4 ; mécanisme natif v4 `@theme` CSS ; retour délibéré à Tailwind v3), recommandation explicite pour l'option native v4, justifiée par `ENGINEERING_GUIDE.md` §1.1 (pas de migration à gérer, donc pas de bénéfice à la couche de compatibilité) et `PROJECT_CHARTER.md` §2 (« le produit doit vieillir bien »). **Statut `Proposé`, non exécuté** — en attente de validation explicite de l'utilisateur avant toute mise à jour de `DESIGN_SYSTEM_ARCHITECTURE.md` ou implémentation du preset lui-même.
- **Aucun fichier de TASK-015 créé** — le preset Tailwind lui-même n'existe pas encore, conformément à « ne rien implémenter ». L'installation exploratoire de `tailwindcss` à la racine (utilisée uniquement pour inspecter les exports du package) a été retirée après investigation — aucune trace de code laissée par cette exploration.
- **Gap mineur trouvé au passage, signalé sans corriger (hors périmètre d'une Task arrêtée)** : `package.json` racine contient encore `typescript-eslint`/`eslint-plugin-jsx-a11y`/`eslint-plugin-import`/`eslint-plugin-boundaries` en devDependencies, installés lors de l'exploration initiale de TASK-014 puis rendus redondants une fois ces mêmes paquets ajoutés aux dépendances propres de `packages/config` — fonctionnellement inutiles à la racine (seuls `eslint`/`typescript`/`turbo` y sont réellement nécessaires pour la résolution de binaire `pnpm exec`). Non nettoyé ici, hors périmètre de cette Task.
- **ADR** : ADR-0001 lui-même est la réponse à cette question — pas d'ADR distinct nécessaire pour la question « faut-il créer un ADR ».
- **Tests/Documentation/Vérifications** : sans objet — aucune implémentation n'a eu lieu.
- Régression vérifiée après le retrait de l'installation exploratoire : `node --test scripts/lib/doc-graph.test.mjs` → 11/11 ; `node scripts/verify-docs.mjs` → 244 documents (le nouvel ADR vit dans `docs/adr/`, non scanné par ce script, cohérent avec `docs/adr/` comme emplacement séparé déjà documenté), 0 lien cassé, 0 orphelin.
- **Documentation mise à jour** : `docs/adr/0001-tailwind-v4-theme-mechanism.md` créé ; `docs/ENGINEERING_BACKLOG.md` §1/§3 (M1 marqué bloqué, TASK-015 arrêtée). `docs/DESIGN_SYSTEM_ARCHITECTURE.md` **non modifié** — la mise à jour proposée dans l'ADR (§ « Impact sur les documents existants ») n'est appliquée qu'une fois l'ADR accepté, jamais en anticipation.
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : décision de l'utilisateur attendue sur ADR-0001 avant de pouvoir reprendre TASK-015 — aucune Task suivante ne peut être anticipée tant que ce blocage n'est pas levé, conformément à l'instruction explicite de ne rien implémenter face à une incohérence d'architecture non résolue.

### ADR-0001 accepté — TASK-015 reprise et terminée (2026-08-05)

- Confirmation explicite de l'utilisateur : « Je valide officiellement ADR-0001. Décision : ACCEPTED... Nous refusons toute couche de compatibilité avec Tailwind v3. » — Option B (mécanisme natif v4) confirmée, aucune autre décision d'architecture à toucher.
- **ADR-0001 mis à jour** : statut `Proposé` → `Accepté`, ligne « Non exécutée » remplacée par la confirmation d'exécution dans le même lot (conforme à `docs/ADR_TEMPLATE.md` §2.4, « le document fondateur concerné est mis à jour dans la même PR que l'ADR, jamais en différé »).
- **Recherche de l'impact réel avant modification** : `grep` de « tailwind.config / preset Tailwind / Tailwind CSS » sur tout `docs/` — seul `docs/DESIGN_SYSTEM_ARCHITECTURE.md` §1 contient une phrase spécifique au mécanisme JS ; `docs/ARCHITECTURE.md` §3, `docs/CONFIGURATION_GUIDE.md`, `docs/TECH_STACK.md` mentionnent « preset Tailwind »/« Tailwind CSS » sans spécifier de mécanisme — aucun changement nécessaire, conforme à l'analyse déjà faite dans l'ADR lui-même. `docs/DESIGN_SYSTEM_ARCHITECTURE.md` §4 (breakpoint `tablet`) laissé inchangé — ne nomme aucun mécanisme, hors du périmètre explicitement déclaré par l'ADR (« Ne modifier aucune autre décision d'architecture »).
- **`docs/DESIGN_SYSTEM_ARCHITECTURE.md` §1 mis à jour** : « extension du thème Tailwind (`tailwind.config`) » → « extension native du thème Tailwind (blocs `@theme` CSS, Tailwind CSS v4) », référence explicite à ADR-0001 ajoutée. **Erreur immédiatement trouvée et corrigée** : première tentative avec un wikilink `[[ADR-0001]]` — ne correspond à aucun fichier réel de `docs/*.md` (les ADR vivent dans `docs/adr/`, hors du graphe de wikilinks), aurait cassé `scripts/verify-docs.mjs` — corrigé en référence texte plate, même correction déjà appliquée à `docs/ENGINEERING_BACKLOG.md` au tour précédent.
- **TASK-015 reprise et terminée** : `packages/config/tailwind-theme.css` créé — bloc `@theme {}` volontairement vide (aucune valeur de token, `DESIGN_TOKENS.md`/`COLOR_SYSTEM.md` restent v1 non finale, réservé à TASK-019/020, pas anticipé ici) ; `tailwindcss@4.3.3` installé en dépendance de `packages/config`.
- **Validation empirique réelle, pas supposée** : `@tailwindcss/cli` d'abord installé pour tester, mais bloqué par la politique de sécurité pnpm sur les scripts d'installation natifs (`@parcel/watcher`, dépendance native de la fonctionnalité `--watch`) — retiré plutôt qu'approuvé à l'aveugle, remplacé par `@tailwindcss/postcss` + `postcss` pour une validation ponctuelle sans mode watch. `@parcel/watcher` s'est révélé être une dépendance transitive de `@tailwindcss/postcss` lui-même (pas seulement du CLI) — approuvé explicitement (`pnpm-workspace.yaml` `allowBuilds`) après vérification qu'il s'agit d'un paquet natif réputé et largement utilisé (écosystème Parcel, utilisé notamment par VS Code), pas une dépendance introduite par un choix inhabituel de ma part. Un script Node temporaire a compilé `@import "tailwindcss"; @import "./tailwind-theme.css"; .probe { @apply text-red-500; }` via PostCSS : compilation réussie, règle `.probe` correctement générée — confirme que le fichier partagé s'importe et fonctionne réellement, pas seulement une syntaxe qui semble correcte.
- **Nettoyage après validation** : `@tailwindcss/postcss`/`postcss` retirés après usage — les garder aurait présupposé prématurément que la future intégration Vite (`apps/web`, tâche non encore décomposée) utilisera PostCSS plutôt que le plugin `@tailwindcss/vite`, une décision qui n'appartient pas à TASK-015. L'entrée `allowBuilds: '@parcel/watcher'` devenue orpheline après ce retrait a été supprimée de `pnpm-workspace.yaml` (vérifié : plus aucune trace de `@parcel/watcher` dans `pnpm-lock.yaml`). Script de validation temporaire supprimé.
- **Sécurité** : `pnpm audit` → 0 vulnérabilité. Approbation du script natif `@parcel/watcher` documentée explicitement ci-dessus plutôt que silencieuse.
- **Tests** : aucun test automatisé propre (fichier CSS déclaratif, `@theme` vide) — vérifié par compilation PostCSS réelle (ci-dessus), même logique que la vérification `pnpm install`/`turbo build` des Tasks précédentes. Suite existante réexécutée : `node --test scripts/lib/doc-graph.test.mjs` → 11/11 ; `node scripts/verify-docs.mjs` → 244 documents, 0 lien cassé, 0 orphelin ; `turbo build lint` → 4/4 lint réussis, build toujours 0 tâche (cohérent, aucun script `build`).
- **Documentation mise à jour** : `docs/adr/0001-tailwind-v4-theme-mechanism.md` (Accepté) ; `docs/DESIGN_SYSTEM_ARCHITECTURE.md` §1 (version 0.3.0) ; `docs/ENGINEERING_BACKLOG.md` §1/§3 (FEATURE-005 close, prochaine action → TASK-016).
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (conforme à la décision ADR-0001 acceptée, aucune autre décision touchée) ; build (validé par compilation PostCSS réelle) ; lint (4/4 packages verts) ; tests (11/11) ; sécurité (`pnpm audit` clean, approbation de script natif documentée) ; performance (non applicable) ; documentation relue, wikilink erroné corrigé avant de conclure.
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : TASK-016 (Job GitHub Actions `lint`, dépend de TASK-014) est la prochaine Task exploitable — non commencée, conformément à l'instruction explicite de ne pas anticiper. Le choix entre `@tailwindcss/vite`/`@tailwindcss/postcss` pour la future intégration de build reste une décision ouverte, à trancher quand `apps/web` sera réellement configuré (rolling wave, pas encore décomposé).

### ADR-0002 — Gap de planification confirmé et corrigé (2026-08-05)

- Confirmation explicite de l'utilisateur, suite à la revue d'avancement M1 du tour précédent : « Je souhaite que tu réalises une revue complète du backlog afin de déterminer s'il s'agit d'un oubli de planification. Si ce gap est confirmé : créer un ADR ; proposer la nouvelle Epic/Feature/Story/Tasks... Je souhaite uniquement corriger le plan d'ingénierie. » — lu comme une autorisation explicite à éditer les documents de planification (contrairement au tour précédent, purement analytique), tout en excluant strictement le code applicatif (« ne modifie aucun code, ne crée aucune implémentation »).
- **Revue complète effectuée avant toute conclusion** : recherche exhaustive (`grep`) de « apps/web », « apps/desktop », « vite.config », « main.tsx », « src-tauri », « tauri.conf », « bootstrap », « point d'entrée », « première fenêtre » dans l'ensemble de `docs/` — confirmé qu'aucune Task ni Feature (M0-M14) ne couvre ce sujet. Vérifié explicitement que ce n'est pas un report déjà assumé : `MVP_ROADMAP.md` §3 diffère `apps/mobile` de façon documentée (non concerné ici) ; `MODULES.md` ne couvre que les modules de fonctionnalité, jamais le shell applicatif ; FEATURE-079/080/081 (EPIC-015, packaging de release) supposent `apps/web`/`apps/desktop` déjà fonctionnels, ne peuvent pas être la réponse puisque le développement itératif (Storybook, E2E Playwright, écrans dès M3) en a besoin bien avant M14. **Gap confirmé comme un oubli de planification**, pas un report délibéré.
- **`docs/adr/0002-application-bootstrap-gap.md` créé** (Accepté directement, l'utilisateur ayant demandé la correction du plan, pas une proposition à valider séparément) — 3 options de positionnement comparées : (A) nouveau Feature dans EPIC-002/M1 — écarté, créerait une dépendance croisée EPIC-002→EPIC-003 (TASK-047 a besoin d'`AppProviders`, TASK-033, qui appartient à EPIC-003) cassant la séquence stricte des Epics ; (B) nouvel Epic dédié avec renumérotation M2-M14 → M3-M15 — écarté, coût de renumérotation en cascade disproportionné à l'ampleur réelle (4 Tasks, 8 points) ; (C) **retenue** — nouveau Feature à la fin d'EPIC-003 (M2), après FEATURE-012, aucune dépendance croisée, aucune renumérotation.
- **Backlog corrigé, documents de planification uniquement, aucun code** :
  - `docs/FEATURES.md` — FEATURE-083 ajoutée sous EPIC-003.
  - `docs/TASK_BREAKDOWN.md` §7 — STORY-023 (TASK-046 `vite.config.ts`, TASK-047 `main.tsx`/point de montage) + STORY-024 (TASK-048 shell Tauri, TASK-049 vérification fenêtre réelle) ; §1 méthodologie mise à jour (17→18 Features, 45→49 Tasks M0-M3). `apps/mobile` explicitement exclu (cohérent `MVP_ROADMAP.md` §3).
  - `docs/DEPENDENCY_GRAPH.md` §4 — nouvelles arêtes (TASK-012/015→046→047, TASK-033/034→047, 047→048→049) ; §3 note ajoutée : le chemin critique au niveau Epic ne change pas (EPIC-003 reste un nœud unique, seule sa durée interne augmente).
  - `docs/MILESTONES.md` §1 — critère de sortie de M2 étendu : exige désormais le rendu réel Web + Desktop, pas seulement l'existence des packages.
  - `docs/EPICS.md` §2 — portée d'EPIC-003 complétée.
  - `docs/ENGINEERING_BACKLOG.md` §1/§3 — compteurs mis à jour (83 Features, 49 Tasks M0-M3).
  - **`docs/IMPLEMENTATION_ROADMAP.md` §5 corrigé** : sa propre auto-revue affirmait « toutes les fonctionnalités documentées apparaissent dans le backlog » et « aucune tâche orpheline » — ces affirmations n'ont pas détecté ce gap réel ; note de correction ajoutée plutôt que laisser une affirmation devenue fausse, cohérent avec `DOCUMENTATION_GUIDE.md` §5 (contradiction signalée explicitement, jamais en silence).
- **Deuxième erreur de wikilink `[[../adr-note|ADR-0002]]` faite puis corrigée immédiatement** dans `FEATURES.md` (même erreur que pour `[[ADR-0001]]` deux tours plus tôt — les ADR vivent hors du graphe de wikilinks de `docs/`) — troisième occurrence de cette même erreur dans la session, corrigée avant vérification plutôt qu'après.
- **Aucun code applicatif touché** — vérifié explicitement via `git status` : tous les fichiers `packages/`/`apps/`/`scripts/` restent dans l'état où TASK-006 à 015 les avaient laissés, aucun marqué modifié ce tour-ci.
- Vérification : `node scripts/verify-docs.mjs` → 244 documents, 6 687 wikilinks, 0 lien cassé, 0 orphelin ; `node --test scripts/lib/doc-graph.test.mjs` → 11/11.
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : TASK-016 reste la prochaine Task exploitable pour poursuivre M1 (CI lint) — non commencée. La nouvelle FEATURE-083 (TASK-046 à 049) devient exploitable une fois M1 sorti ET TASK-033/034 terminées (EPIC-003). Gap connexe signalé dans l'ADR-0002 mais non traité : `CI_CD_GUIDE.md` §1 décrit un job CI `build` sans Task dédiée dans le backlog actuel — hors périmètre de cette revue, candidat pour une future correction similaire.

### Règle n°1 — Definition of Done Epic, formalisée en règle permanente (2026-08-05)

- L'utilisateur a établi une règle de session (« Règle n°1 » : avant de clôturer une Epic, vérifier point d'entrée/point de sortie/artefact observable/critère de validation/démonstration possible, sinon STOP + ADR) — appliquée immédiatement à titre de validation, pas seulement notée : EPIC-001 (M0, déjà clos) et EPIC-002 (M1, en cours) confirmés conformes ; EPIC-003 (M2) confirmé comme ayant précisément échoué à cette règle *avant* ADR-0002 (aucun artefact observable, aucune démonstration possible), désormais conforme grâce à FEATURE-083 ; EPIC-004 (M3) confirmé conforme, sa démonstration (connexion à un vrai serveur) dépendant directement de la correction d'EPIC-003.
- Question posée en fin de tour précédent (formaliser dans `MILESTONES.md` ou `DEFINITION_OF_DONE.md`, ou rester une règle non écrite) — l'utilisateur a tranché explicitement : **`DEFINITION_OF_DONE.md`**, avec justification propre (« MILESTONES.md définit les critères de sortie des jalons ; DEFINITION_OF_DONE.md définit les critères de qualité/complétude applicables à toutes les Epics/Features/Tasks »), cohérente avec la distinction déjà actée dans le corpus entre ces deux documents.
- **`docs/DEFINITION_OF_DONE.md` mis à jour** : nouvelle section « Definition of Done — Epic » ajoutée avant la checklist Code/Documentation/Tests existante (gate antérieur : une Epic doit satisfaire cette section avant même que ses Tasks ne commencent, pas seulement avant un merge de PR) — 5 critères repris tels que formulés par l'utilisateur, note de provenance vers ADR-0002, distinction explicite avec `MILESTONES.md` rappelée dans le texte, usage systématique en revue d'architecture explicité. Version 0.1.0 → 0.2.0.
- **`docs/adr/0002-application-bootstrap-gap.md` mis à jour en cohérence** : champ « Documents impactés » et section « Impact sur les documents existants » complétés avec `IMPLEMENTATION_ROADMAP.md` (déjà fait au tour précédent, oublié dans la liste d'en-tête — corrigé) et `DEFINITION_OF_DONE.md` (nouvelle conséquence de cette ADR, la règle qui l'a détectée devient permanente).
- Aucun autre document touché (`CODE_REVIEW_GUIDE.md`/`CHECKLISTS.md` non modifiés — l'utilisateur a demandé explicitement `DEFINITION_OF_DONE.md` uniquement, pas d'extension non sollicitée).
- Vérification : `node scripts/verify-docs.mjs` → 244 documents, 6 688 wikilinks, 0 lien cassé, 0 orphelin ; `node --test scripts/lib/doc-graph.test.mjs` → 11/11.
- Aucun code applicatif touché.
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : cette nouvelle checklist Epic s'appliquera à FEATURE-083 (déjà conforme par construction, voir ADR-0002) et à toute future décomposition d'Epic (M4-M14, rolling wave) avant que leurs Tasks ne soient exploitables. TASK-016 reste la prochaine Task exploitable pour poursuivre M1.

### Premier push vers `origin/main` (2026-08-05)

- Confirmation explicite de l'utilisateur : « pousse tout vers github et met ton claude.md à jour. » — première mise à jour du dépôt distant depuis le commit de checkpoint `3023a4c` (Phases 5-13, jamais poussé).
- **Vérifications avant commit** : `git status` passé en revue intégralement (95 entrées) — aucun fichier sensible (`.env`, secrets, credentials, clés) ; `node_modules/`/`.turbo/` correctement exclus par `.gitignore` à toute profondeur (confirmé via `find` sur le disque vs `git status`, qui ne les liste pas) ; suite de tests et `scripts/verify-docs.mjs` réexécutés une dernière fois avant commit — 244 documents, 0 lien cassé, 0 orphelin, 11/11 tests.
- **Contenu du commit** : l'intégralité du travail non commité à ce jour — documentation des phases post-Phase 13 (Architecture d'état, Moteur Audio, Moteur de Recherche, Plateforme Offline, Engineering Handbook, Implementation Plan, Developer Playbook, Revue de consolidation), TASK-001 à TASK-015 (premier code applicatif : `scripts/`, `pnpm-workspace.yaml`, `turbo.json`, squelettes des 4 packages, configuration partagée), ADR-0001 (Tailwind v4), ADR-0002 (gap de bootstrap applicatif, FEATURE-083), et la règle permanente « Definition of Done — Epic ».
- Commité sur `main` puis poussé vers `origin/main` (`https://github.com/LeKdeX/Melodia.git`).
- Ouvert : TASK-016 reste la prochaine Task exploitable. Le dépôt distant reflète désormais l'état réel du projet — les futures Tasks pourront être committées plus fréquemment plutôt qu'en un unique lot rétroactif.

### TASK-016 — Job GitHub Actions `lint` (2026-08-05)

- Instruction explicite (« dernière ligne droite de M1 » : TASK-016, TASK-017, TASK-018 successivement, workflow d'ingénierie à 6 étapes par Task, périmètre strict, backlog non modifiable). Spécification lue (`docs/TASK_BREAKDOWN.md` §6 : « Job GitHub Actions `lint` », S, dépend de TASK-014 — revérifiée terminée). Document de référence lu : `docs/CI_CD_GUIDE.md` §1 (pipeline PR, job `lint` décrit comme « ESLint + Prettier --check, tout le monorepo via Turborepo, cache incrémental »).
- **Gap réel trouvé en vérifiant les dépendances, signalé plutôt que masqué ou silencieusement élargi** : `docs/CI_CD_GUIDE.md` §1 mentionne Prettier dans la description du job `lint`, mais recherche exhaustive (`grep`) de « Prettier »/« prettier » dans `docs/TASK_BREAKDOWN.md` → aucune occurrence. Aucune Task (TASK-014 incluse, dont le titre et le périmètre réel sont strictement « ESLint partagé ») n'a jamais installé ni configuré Prettier. Conformément à la contrainte explicite de ce tour (« respecter strictement le périmètre de chaque Task », « ne pas modifier le backlog », « ne pas créer de nouvelles Features ») : le job CI implémente ce qui existe réellement (ESLint via `turbo lint`), Prettier est exclu et signalé comme gap connexe non traité — même traitement déjà appliqué au job CI `build` dans ADR-0002.
- **ADR** : aucun nécessaire — ce n'est pas une incohérence d'architecture (aucune décision structurante contredite), seulement un écart entre la description aspirationnelle d'un document cible (`CI_CD_GUIDE.md`) et la décomposition réelle du backlog, déjà le type de gap explicitement traité par simple signalement dans ADR-0002 lui-même.
- **Résultat** : `.github/workflows/ci.yml` créé (le dossier `.github/` n'existait pas encore, confirmé avant création) — déclenché sur `pull_request` et `push` vers `main` (cohérent avec `docs/GIT_WORKFLOW.md` §3, CI verte obligatoire avant merge). Un seul job `lint` : `actions/checkout@v4` → `pnpm/action-setup@v4` (version lue depuis le champ `packageManager` de `package.json`, pas dupliquée en dur) → `actions/setup-node@v4` (Node 22, cache `pnpm`) → `pnpm install --frozen-lockfile` → `pnpm turbo lint`. Structuré comme un unique fichier de pipeline à jobs multiples (pas un fichier par job) — cohérent avec `docs/CI_CD_GUIDE.md` §1, qui décrit tous les jobs comme des branches parallèles d'un seul pipeline ; TASK-017 ajoutera son job `typecheck` au même fichier plutôt que d'en créer un second.
- **Validation empirique réelle, pas supposée** : `pnpm install --frozen-lockfile` → succès, lockfile synchronisé. `pnpm turbo lint` (commande exacte utilisée par le workflow) → 4/4 packages réussis, d'abord avec cache (« FULL TURBO »), puis reforcé avec `--force` pour confirmer un passage réellement à neuf (0 caché, 4/4 réussis, ~2.8s) — élimine le risque de rejouer un succès en cache périmé. Contenu du fichier YAML vérifié structurellement (présence de toutes les clés attendues) faute de parseur YAML disponible dans le dépôt (aucune dépendance ajoutée pour ce seul besoin, hors périmètre).
- **Sécurité** : aucune nouvelle dépendance applicative — uniquement des actions GitHub officielles/largement adoptées (`actions/checkout`, `actions/setup-node`) et l'action déjà implicitement nécessaire `pnpm/action-setup`. Aucun secret utilisé.
- **Tests** : aucun test automatisé propre (fichier de configuration CI déclaratif) — vérifié par exécution réelle des commandes qu'il invoque (ci-dessus), même logique que TASK-015. Suite existante réexécutée : `node --test scripts/lib/doc-graph.test.mjs` → 11/11 ; `node scripts/verify-docs.mjs` → 244 documents, 0 lien cassé, 0 orphelin (`.github/` hors du graphe de wikilinks, cohérent).
- **Documentation mise à jour** : `docs/ENGINEERING_BACKLOG.md` §1/§3/§6 uniquement — conforme à la contrainte explicite de ne pas modifier `TASK_BREAKDOWN.md`/`FEATURES.md`/`CI_CD_GUIDE.md` ce tour-ci ; le gap Prettier reste documenté ici et dans `ENGINEERING_BACKLOG.md`, pas corrigé silencieusement dans `CI_CD_GUIDE.md`.
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (conforme, aucune frontière touchée) ; build (`pnpm install --frozen-lockfile` clean) ; lint (4/4 packages verts, vérifié à froid) ; tests (11/11) ; sécurité (aucune dépendance applicative ajoutée) ; documentation relue, aucun wikilink introduit vers `.github/` (hors scope du graphe).
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : TASK-017 (Job GitHub Actions `typecheck`, dépend de TASK-013) est la prochaine Task exploitable — non commencée. Gap Prettier toujours signalé, non traité (créer une Task dédiée sortirait du périmètre « ne pas modifier le backlog » de ce tour). Gap connexe du job `build` (ADR-0002) toujours non traité, inchangé.

### TASK-017 — Job GitHub Actions `typecheck` (2026-08-05)

- Suite directe de TASK-016, même instruction-cadre (« dernière ligne droite de M1 »). Spécification lue (`docs/TASK_BREAKDOWN.md` §6 : « Job GitHub Actions `typecheck` (tsc --noEmit, par package) », S, dépend de TASK-013 — revérifiée terminée). `docs/CI_CD_GUIDE.md` §1 relu (« typecheck (tsc --noEmit, par package) »). `docs/FEATURES.md` : FEATURE-006 confirmée scopée strictement à « CI de base (lint, typecheck) », aucune mention de build/test/e2e — cohérent avec le périmètre exact de TASK-016+017, aucun gap de couverture supplémentaire à ce niveau.
- **État initial vérifié avant d'écrire quoi que ce soit** : aucun script `typecheck` dans les 4 packages, aucune tâche `typecheck` dans `turbo.json`, `typescript` présent uniquement en devDependency racine (jamais dans les 4 packages eux-mêmes) — à la différence d'`eslint`, déjà ajouté explicitement à chaque package par TASK-014 (résolution stricte pnpm, pas de hissage implicite des binaires).
- **Gap réel trouvé en exécutant la validation, résolu dans le périmètre de la Task (pas un STOP+ADR)** : une fois le script `typecheck: tsc --noEmit` câblé, `tsc --noEmit` échoue avec `TS18003` (« No inputs were found ») sur les 4 packages — leurs dossiers `src/` ne contiennent que des `.gitkeep`, aucun fichier `.ts` réel. Ce même symptôme avait déjà été rencontré une fois lors de TASK-013/014, mais traité alors comme une vérification ponctuelle (fichier temporaire créé puis supprimé) puisqu'il s'agissait d'un contrôle unique, pas d'un job CI destiné à rester vert en continu. Ici la nature change : un job CI qui échoue structurellement à chaque commit tant qu'aucune Task n'a ajouté de source réelle bloquerait tout merge (`docs/GIT_WORKFLOW.md` §3, CI verte obligatoire) et rendrait TASK-018 (« vérifier CI verte ») impossible à valider honnêtement. **Analyse explicite avant de choisir une solution** : ce n'est pas une incohérence d'architecture (aucune décision structurante contredite, aucune Feature en conflit) — c'est un vide temporel attendu (les packages n'ont intentionnellement aucune source réelle avant le rolling wave M4+), donc traité comme une décision d'implémentation justifiée plutôt qu'escaladé en ADR, cohérent avec la distinction déjà établie dans cette session entre les deux catégories.
- **Solution retenue, documentée explicitement plutôt que masquée** : ajout d'un fichier `src/index.ts` minimal (`export {};`, avec commentaire expliquant sa nature temporaire) à chacun des 4 packages — un placeholder de compilation pour `tsc`, strictement analogue à ce que `.gitkeep` est déjà pour Git (déjà accepté sans controverse par TASK-008 à 012). Aucune logique métier, aucune décision produit — jugé ne pas constituer une anticipation de M2 malgré l'instruction explicite « ne pas anticiper M2 » de ce tour, dans la mesure où le fichier ne contient aucune fonctionnalité, seulement une déclaration de module vide nécessaire au fonctionnement de l'outillage TypeScript lui-même.
- **Résultat** :
  - `turbo.json` — tâche `typecheck` ajoutée (`dependsOn: ["^build"]`, même forme que `lint`/`test`).
  - `packages/{core,ui,platform,app}/package.json` — script `"typecheck": "tsc --noEmit"` + devDependency `"typescript": "6.0.3"` ajoutés (miroir exact du pattern déjà établi pour `eslint` par TASK-014).
  - `packages/{core,ui,platform,app}/src/index.ts` — placeholder de compilation créé dans chacun.
  - `.github/workflows/ci.yml` — second job `typecheck` ajouté au même fichier de pipeline (checkout → pnpm/action-setup → setup-node → `pnpm install --frozen-lockfile` → `pnpm turbo typecheck`), pas un fichier séparé, cohérent avec le choix structurel déjà pris en TASK-016.
- **Validation empirique réelle, pas supposée** : `pnpm install` → `tsc` correctement lié dans `node_modules/.bin` de chacun des 4 packages (vérifié explicitement) ; `tsc --noEmit` sur `packages/core` seul reproduit `TS18003` avant le fix, confirmé résolu après ; `pnpm turbo typecheck --force` (commande exacte de la CI, à froid, 0 caché) → 4/4 réussis ; `pnpm turbo lint --force` réexécuté après l'ajout des `index.ts` pour confirmer l'absence de régression → 4/4 toujours verts ; `pnpm audit` → 0 vulnérabilité après ajout de `typescript` en dépendance directe des 4 packages.
- **Sécurité** : aucune nouvelle dépendance externe autre que `typescript`, déjà auditée et pinnée depuis TASK-013/014 (`^6.0.3` → `6.0.3` exact, cohérent avec le pin déjà en place).
- **Tests** : aucun test automatisé propre (configuration CI/tooling) — vérifié par exécution réelle (ci-dessus). Suite existante réexécutée : `node --test scripts/lib/doc-graph.test.mjs` → 11/11 ; `node scripts/verify-docs.mjs` → 244 documents, 0 lien cassé, 0 orphelin.
- **Documentation mise à jour** : `docs/ENGINEERING_BACKLOG.md` §1/§3/§6 (FEATURE-006 close, prochaine action → TASK-018). `docs/TASK_BREAKDOWN.md`/`docs/FEATURES.md`/`docs/CI_CD_GUIDE.md` non modifiés, conformément à la contrainte explicite de ne pas toucher le backlog ce tour.
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (conforme, aucune frontière touchée, placeholder neutre) ; build (`pnpm install` clean) ; lint (4/4 verts, revalidé après ajout des placeholders) ; typecheck (4/4 verts, à froid) ; tests (11/11) ; sécurité (`pnpm audit` clean) ; documentation relue.
- Non commité, non poussé sur `origin/main` à ce stade.
- Ouvert : TASK-018 (Vérifier CI verte sur commit vide — clôture M1, dépend de TASK-016+017) est la prochaine et dernière Task de M1 — non commencée. Gap Prettier (CI_CD_GUIDE.md §1) et gap `build` (ADR-0002) toujours non traités, inchangés.

### TASK-018 — CI verte confirmée, M1 officiellement sorti (2026-08-06)

- Suite directe de TASK-016/017. Spécification lue (`docs/TASK_BREAKDOWN.md` §6 : « Vérifier CI verte sur commit vide — clôture M1 », XS, dépend de TASK-016+017 — toutes deux revérifiées terminées).
- **Décision explicite demandée à l'utilisateur avant d'agir** : le titre littéral de la Task implique un vrai run GitHub Actions — impossible sans commit+push, alors qu'aucune autorisation de push n'était en vigueur ce tour (règle permanente : une autorisation de push ne vaut que pour la demande explicite qui l'a accordée, jamais par défaut). Proposé deux options via question structurée (commiter+pousser maintenant vs. rester en local sur la seule base des validations `turbo lint`/`turbo typecheck` déjà faites) — **l'utilisateur a choisi de commiter et pousser**.
- **Commit et push exécutés** : `git status` revu intégralement avant staging (aucun fichier inattendu/sensible) ; commit `40430ab` (« feat(ci): add GitHub Actions lint + typecheck jobs (TASK-016, TASK-017) », 13 fichiers) poussé vers `origin/main` — déclenchant un run réel du workflow CI créé par TASK-016/017.
- **Limite d'environnement signalée avant toute conclusion, pas après** : ni `gh` CLI ni token GitHub authentifié disponibles dans ce sandbox (`gh --version` → command not found ; l'API REST publique non authentifiée est rate-limited). Impossible de vérifier soi-même le résultat du run — demandé explicitement à l'utilisateur de consulter `https://github.com/LeKdeX/Melodia/actions` et de rapporter le résultat, plutôt que de prétendre à une vérification jamais faite (cohérent avec la règle d'honnêteté du projet, `CLAUDE.md` en-tête : « je ne prétends jamais avoir vérifié ce qui ne l'a pas été »).
- **Confirmation externe reçue de l'utilisateur** : « Le workflow GitHub Actions est vert. » — traitée comme la vérification empirique réelle exigée par le titre de la Task, seule source possible dans cet environnement.
- **Revalidation locale finale avant clôture** (dernière garde-fou avant de déclarer M1 sorti) : `pnpm turbo lint typecheck --force` (à froid, 0 caché) → 8/8 tâches réussies (4 packages × 2 jobs) ; `node scripts/verify-docs.mjs` → 244 documents, 0 lien cassé, 0 orphelin ; `node --test scripts/lib/doc-graph.test.mjs` → 11/11.
- **Documentation mise à jour** : `docs/ENGINEERING_BACKLOG.md` §1/§3/§6 (M1 marqué **Sorti**, FEATURE-006 confirmée close, prochaine action → M2 non commencé).
- Auto-review effectuée (`docs/DEVELOPER_PLAYBOOK.md` §8) : architecture (aucune touchée) ; build (non applicable, aucun script `build` encore) ; lint/typecheck (8/8 verts, à froid, + CI réelle verte confirmée) ; tests (11/11) ; sécurité (aucune nouvelle dépendance) ; documentation relue.
- Commité et poussé sur `origin/main` (commit `40430ab`, fait dans le cadre de cette Task elle-même).
- **M1 (« Workspace ») officiellement clos.** Rapport de clôture complet ci-dessous.

### Rapport de clôture — Milestone M1 « Workspace » (2026-08-06)

**Critère de sortie officiel** (`docs/MILESTONES.md` §1) : « Monorepo pnpm/Turborepo installé, CI de base (lint/typecheck) verte sur un commit vide » — **satisfait dans son intégralité.**

**1. Résumé des livrables** — 4 Features (EPIC-002), 13 Tasks (TASK-006 à TASK-018), toutes terminées :
- **FEATURE-003** (Initialisation monorepo) : `pnpm-workspace.yaml`, `turbo.json`, arborescence `apps/{web,desktop,mobile}` (TASK-006/007/008).
- **FEATURE-004** (Squelette des 4 packages) : `@melodia/{core,ui,platform,app}` — `package.json`, `tsconfig.json`, dossiers `src/` conformes à `ARCHITECTURE.md` §1-2 (TASK-009 à 012).
- **FEATURE-005** (Configuration partagée) : `packages/config` — `tsconfig.base.json` (strict + `noUncheckedIndexedAccess`), `eslint-preset.js` (flat config ESLint 9), `tailwind-theme.css` (Tailwind v4 natif, ADR-0001) (TASK-013 à 015).
- **FEATURE-006** (CI de base) : `.github/workflows/ci.yml` — jobs `lint` et `typecheck`, vérifiés verts en conditions réelles sur `origin/main` (TASK-016 à 018).

**2. État du workspace** — `pnpm-workspace.yaml` (`apps/*`, `packages/*`), `turbo.json` (tâches `build`/`lint`/`typecheck`/`test`/`dev`), `package.json` racine (`packageManager: pnpm@11.20.0`). `pnpm install` propre (`Already up to date` en état stable). Limite d'environnement persistante : pnpm accessible uniquement via `npx pnpm` sur cette machine (pas d'installation globale, `corepack enable` bloqué par des droits administrateur absents) — sans impact fonctionnel, contournement stable depuis TASK-006.

**3. État des packages** — 5 packages workspace : `@melodia/{core,ui,platform,app,config}`. Les 4 premiers ont un `src/` avec un seul fichier réel (`index.ts`, placeholder de compilation `export {};`, TASK-017) — aucune logique métier n'existe encore nulle part dans le monorepo, voulu et conforme au rolling wave (`TASK_BREAKDOWN.md` §1, décomposition Task complète réservée à M0-M3 uniquement pour le squelette, le code applicatif réel commence au fil de M2+).

**4. État de la CI** — `.github/workflows/ci.yml`, un seul pipeline à 2 jobs (`lint`, `typecheck`), déclenché sur `pull_request` et `push` vers `main`. **Vérifié vert en conditions réelles** sur le commit `40430ab` (confirmation externe de l'utilisateur — ni `gh` CLI ni token GitHub authentifié disponibles dans cet environnement pour une vérification autonome, limite signalée explicitement avant la confirmation). Job `build` absent (gap connexe signalé dans ADR-0002, non traité — aucune Task ne le couvre à ce jour). Prettier absent du job `lint` malgré sa mention dans `CI_CD_GUIDE.md` §1 (gap signalé lors de TASK-016, non corrigé — aucune Task ne l'a jamais installé).

**5. État du lint** — ESLint 9 (flat config), preset partagé (`@typescript-eslint/no-explicit-any` en erreur, `import/order`, 3 règles `jsx-a11y` en erreur, `eslint-plugin-boundaries` enregistré mais sans règle active — préparé pour une future Task d'activation). `pnpm turbo lint --force` → 4/4 packages verts, à froid, revérifié à la clôture.

**6. État des tests** — Suite unique du projet à ce jour : `scripts/lib/doc-graph.test.mjs` (11 tests, logique de `scripts/verify-docs.mjs`) → 11/11. Aucun test applicatif (Vitest/Playwright) — normal, aucun code applicatif n'existe encore ; `docs/TESTING_STRATEGY.md` s'appliquera dès que du code réel sera écrit (M2+).

**7. État des ADR** — 2 ADR, tous deux **Accepté** : ADR-0001 (mécanisme natif Tailwind v4 `@theme`, remplace l'hypothèse `tailwind.config` v3 de `DESIGN_SYSTEM_ARCHITECTURE.md`) ; ADR-0002 (gap de planification du bootstrap applicatif, FEATURE-083 ajoutée à EPIC-003/M2). Aucun ADR en attente.

**8. Dette technique restante** (signalée explicitement, aucune corrigée par anticipation) :
- Job CI `build` absent malgré sa mention dans `CI_CD_GUIDE.md` §1 (ADR-0002).
- Prettier absent du job `lint` malgré sa mention dans `CI_CD_GUIDE.md` §1 (TASK-016).
- 6 citations de section non résolues, hors périmètre du top 10 (TASK-002/003/004, jamais traitées).
- 54 désynchronisations mineures de version d'en-tête sur des documents hors top 10 (TASK-002/003).
- `eslint-import-resolver-typescript` absent — `import/no-unresolved` volontairement non activé (TASK-014).
- Package.json racine contient des devDependencies redondantes avec `packages/config` (TASK-015).
- Les 4 placeholders `src/index.ts` (`export {};`) devront être remplacés par du code réel dès que M4+ y introduit des fichiers sources.

**9. Risques connus** — aucun nouveau risque introduit par M1 lui-même. Risques déjà actés dans `docs/RISK_REGISTER_TECHNICAL.md`/`docs/TECHNICAL_RISKS.md` restent valables et non réévalués par cette clôture (hors périmètre de M1).

**10. Préparation de M2** — EPIC-003 (Fondations), 7 Features (dont FEATURE-083, ADR-0002), 20 Tasks déjà décomposées (TASK-019 à 049, dont TASK-046 à 049). Critère de sortie de M2 déjà étendu par ADR-0002 pour exiger un rendu réel Web + Desktop, pas seulement l'existence des packages. **M2 non commencé, conformément à l'instruction explicite de ne pas l'anticiper.**

- Non poussé au-delà du commit `40430ab` déjà sur `origin/main` — les mises à jour de documentation de cette clôture (`ENGINEERING_BACKLOG.md`, `CLAUDE.md`) restent locales, non commitées, en attente d'une demande explicite de commit/push.
